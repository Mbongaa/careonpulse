# Careon Pulse — Project Status & Timeline

**Last updated:** 22 August 2026 · **Companion to:** `docs/platform/PLATFORM_BLUEPRINT.md` (v2.6, authoritative) · **Delivery gaps:** `docs/platform/PLATFORM_GAP_REGISTER.md`

This file tracks where the platform stands: what exists, what is in progress, and what comes next. Agents and developers update it when a milestone changes state. Section references (§) point into the blueprint.

Legend: ✅ done · 🔵 current focus · ⬜ not started

---

## 1. Repository & infrastructure state (today)

### `careonpulse` (this repo) — Module 1: Pulse dashboard — ✅ live

- Next.js 16 / React 19 dashboard app (`src/`), deployed on Vercel; mobile-first, installable PWA
- Supabase (EU) schema in `supabase/migrations/`: org tenancy (`organizations`, `organization_members` with `org_admin`/`member`, platform superadmin), audit events, login rate limiting, auth security hardening (through 26 Jul 2026), the access-token hook `0019` (namespaced `careon` claim), the facturatie schema `0020` (four tables + the private Storage bucket `facturen`, 9 Aug 2026), `0021` (invoice mail log + `mail` quota scope, 13 Aug 2026), `20260821213000` (leading Facturatie foreign-key indexes, 21 Aug 2026), the service-only hash-only mobile handoff ledger/RPCs `20260822210000` + JWT-role compatibility guard `20260822213000`, the forced-RLS encrypted mobile push-device registry `20260822230000`, the service-written organization-scoped TGC worker heartbeat `20260822233000`, `20260822234500` (Facturatie archive-metadata invariants plus private PDF/PNG/25 MB Storage policy), and `20260822235000`/`20260822235500` (security-invoker agenda projection plus service-only Facturatie finalization; legacy authenticated privileged RPC removed, 22 Aug 2026)
- AI-assistent on the hardened OpenAI regime (pinned snapshot, fail-closed moderation, eval suites) — see `AI_OPERATIONS.md`
- Post-login module launcher at `/modules` (27 Jul 2026) — web precursor of the shell tile launcher (§5): Careon Pulse Directie live; YAAZ launches through `NEXT_PUBLIC_YAAZ_URL` and the one-click OIDC handoff. G08 closed 21 Aug 2026: YAAZ `careon-sso` 1.2.0 now keeps a persistent authenticated **Modules** return entry to the Careon launcher, while the dashboard user menu and Facturatie already expose that switcher. Registry: `src/data/careon/careon-modules.ts`. G14 now supplies a responsive CSS YAAZ wordmark without inventing a customer image; it remains one-step replaceable if an approved official asset arrives.
- Native-shell registry and one-time WebView session handoff are live (22 Aug 2026, Careon `a2f9b7f`): `/api/mobile/v1/modules` accepts only signed/current Supabase tokens minted to the dedicated public shell client, reads the caller's own membership under RLS and returns only entitled module targets. `/api/mobile/v1/handoffs` mints a random 60-second single-use capability for that exact user, organization and entitled target; only its SHA-256 digest enters the service-only ledger. The shell POSTs the raw value in memory to `/api/mobile/v1/session`, which revalidates current account/membership/role/target and establishes an HttpOnly Careon session. No platform access/refresh token enters a URL, WebView, log or module. YAAZ `careon-sso` 1.3.1 preserves a bounded local deep link through the normal Careon OIDC flow, so a fresh WebView opens without a second login, and strips ambient secret-bearing request globals from exception-log context.
- Mobile push registration is receiver-ready but deliberately dormant (22 Aug 2026, Careon `3414dfb`, shell `9e3ef5c`): the dedicated public-shell bearer boundary registers or revokes an opted-in installation through `/api/mobile/v1/devices`; raw FCM tokens are AES-256-GCM protected before Supabase and only a keyed digest plus ciphertext enter the service-only forced-RLS registry. Firebase Messaging and Analytics auto-init remain off before employee opt-in, accepted messages contain only a short non-sensitive title plus an entitled `careonpulse://` deep link, and notification taps still require the normal registry and one-time handoff. TGC-owned Firebase/APNs credentials, a trusted sender and real-device delivery acceptance are not yet present, so notification delivery is not production-active.
- The governed mobile file bridge is build-ready (22 Aug 2026, Careon `9b2b5f3` + cache hardening `a914812`, shell `d496ca6`): generated Facturatie PDFs and the Directie sample CSV use an exact shell-only native channel, archived invoice downloads are restricted to the same-origin UUID PDF endpoint, and Android uploads use the OS picker intersected with a fixed business-format/size/count allowlist. Redirects, unsafe filenames, wrong module/MIME/content signatures, oversized payloads and unexpected fields fail closed; temp files are removed after the OS save/share sheet. Normal browsers retain their ordinary download behavior. The packaged Android manifest adds no camera, microphone, location or broad-storage permission. Signed Android/iOS physical-device acceptance remains open.
- Phase 0 meeting work now includes the isolated native bridge proof plus a dormant server foundation (22 Aug 2026, sibling `humhub-meeting-modules` commit `32d4484`): `meeting-core` 0.1.0 adds exact Careon-subject resolution, invitation/session/window authorization, literal rooms and 15-minute RS256 JaaS tokens from a 4096-bit owner-only server key. E-mail is omitted; recording/transcription/livestreaming/file upload/outbound/SIP are disabled and backed by database constraints. Pure checks are **46/46**, transactional HumHub checks **13/13**, syntax/key/health/guest/admin-browser acceptance are green. No production configuration/schema/navigation, real JaaS account/key/token or shipping-shell media permission changed. Production packaging, tenant/DPA/region, native integration and signed physical-device joins remain open.
- Superadmin dashboard brought up to its own spec (29 Jul 2026): role mutations (promote/demote, membership add/remove, account delete), organisation rename/delete, composable audit-log filters (org / user / action / period) with pagination, per-org detail view, and explicit error states instead of silent empty ones — see `docs/AUDIT_2026-07-29.md`
- Facturatie module live at `/facturatie` (9 Aug 2026, D19): admin-only invoicing inside this repo with its own module shell and menu (invoice list, editor with live PDF preview, contacts incl. employee union, template library), schema `0020` + private Storage bucket `facturen`, access limited to `org_admin` + superadmins with org membership on four layers — spec in `agent-handoff/15-facturatie.md`. **Phase B (e-mail dispatch) built 13 Aug 2026**: send an issued invoice as a Resend transactional mail (owner decision 13 Aug, deviating from the EU-sovereign proposal — D19 amendment) with the archived PDF attached, one mail-log row per attempt (`0021`, RLS select-only), rate-limited on the `mail` quota scope — **fail-closed until the DPA**: with the `CAREON_MAIL_` keys empty the route answers 503 and nothing can be sent
- Quality gates: `verify:ci` suites, Playwright e2e + WCAG-AA audits, `RELEASE_GATES.md`, production readiness audit (24 Jul 2026)
- TGC EPD snapshot automation live locally (availability accepted 22 Aug 2026): the authenticated browser runner inventories and refreshes all five full-history exports, applies source field minimisation, validates with the production parsers, pushes atomically to Supabase, verifies central read-back, and runs weekly Monday 06:00 through Windows Task Scheduler. Databron keeps the manual importer and adds **Update imports through AI** with live queued/login/export/validation/upload/verification status. The exact Windows worker now emits a metadata-only heartbeat every 30 seconds; administrators see **beschikbaar**, **niet bereikbaar** or **onbekend**, and an offline request remains safely queued. The Supabase queue/heartbeat are organization-scoped and the TGC credential stays only in the local ignored worker environment — operations and exact portal routes in `docs/TGC_EXPORT_AUTOMATION.md`
- Platform docs: `docs/platform/PLATFORM_BLUEPRINT.md` (v2.6) + this status file + the cross-chat `docs/platform/PLATFORM_GAP_REGISTER.md`; `AGENTS.md` platform section

### Sibling repositories (blueprint §8) — 🔵 in progress (30 Jul 2026)

| Repo | Purpose | Status |
|---|---|---|
| `careonpulse-shell` | Flutter shell app: login, launcher, push, native Jitsi | 🔵 Local Git HEAD `7460991` documents the native-file increment at runtime commit `d496ca6` (push `9e3ef5c`, Android build `beee34b`, handoff `516e2db`): Flutter 3.47.1/Dart 3.13.1, branded Android/iOS projects, public-client OAuth 2.1 PKCE, secure storage/refresh, server-driven launcher, entitlements, deep links, guarded WebViews, one-time WebView session handoff, explicit opt-in FCM receiver/device registration and governed upload/download/share handlers. Analyze + **36/36** tests are green. The checksum-backed OpenJDK 17/API 34+36/NDK/CMake runbook reproduces signed-debug and deliberately unsigned release APKs; packaged id/SDK/backup/cleartext/permission/FCM-auto-init properties and the no-debug-release-signing boundary are verified. Client-owned Firebase/APNs activation, the trusted sender, signed Android/iOS physical-device acceptance and store/MDM rollout remain; remote ownership/visibility still needs to be settled. |
| `humhub-meeting-modules` | `meeting-core` / `meeting-recordings` / `meeting-intelligence` | 🔵 Local Git commit `32d4484` contains the isolated native bridge plus installable, disabled-by-default `meeting-core` 0.1.0. Native checks remain **12/12**; server checks are **46/46** plus **13/13** rollback-only runtime checks. Exact Careon-subject/invitation/window authorization and strict server-only RS256 signing exist. No real JaaS credential, production package/config or production-shell integration exists yet. |
| `platform-deploy` | Hetzner/Coolify Compose stack, env templates, backups, `VERSIONS.md` | ✅ live base at host release `7498990` (off-site recovery runtime `5a9d63c`). It runs `careon-sso` **1.3.1**, `careon-m365` **0.8.0**, `careon-spaces` **1.0.0** and `careon-ui` **1.1.0** with module parity/health checks. Nightly local backups, five-minute full-plane health and hourly external smoke are active. The R2 code now uses completion-last pair manifests, remote size/SHA metadata verification and a no-overwrite exact-pair recovery fetch; `BACKUP_OFFSITE_REQUIRED` is deliberately off until TGC supplies its private EU bucket/token. Calendar-only Outlook writes remain enabled; dormant text-only `Mail.Read`, mail send and file writes remain off. Production fixtures are removed and `careon-demo` is disabled. |

### External services

| Service | Status |
|---|---|
| Supabase project (EU) — identity + dashboard data | ✅ live; OAuth 2.1 server enabled, ES256 signing active, HumHub confidential client verified. The Careon Pulse mobile client `cb42abd3-aed8-49d4-9126-0fa42da9d597` is public (`none`), has no client secret, permits authorization-code + refresh-token grants and only the exact `com.careonpulse.app://oauth/callback` redirect. Auth `site_url` is the canonical `https://www.careonpulse.com`; the current canonical, legacy alias and bounded local development callbacks are explicitly allowlisted so consent and first-party cookies remain on the same production host. |
| Microsoft Entra ID + Graph | ✅ D20 dashboard login, D21 YAAZ delegated integration and D22 employee JIT lifecycle are live. Microsoft is the primary employee login for the complete Careon dashboard and launcher; password access is management/exception access. Three single-tenant apps keep login, personal Graph data and directory inventory in separate permission/failure domains. The stable `All Company` SharePoint library (`/sites/allcompany/Gedeelde documenten`) remains the canonical shared source. Calendar-only delegated writes are enabled. Text-only Outlook message detail is code/deployment ready behind a dormant delegated `Mail.Read` gate; `Mail.Send` and file writes remain off. `acct`/`email`/`xms_edov` claims and assignment-required are active; Hicham, Wida and Zairo have `Careon.User`; Directie renders all 66 identities plus exactly 3 eligible assignments. Existing-account linking, disposable first launch, cross-account isolation and end-to-end offboarding are accepted. ✅ G01/G15; 🔵 G02/G03. |
| Vercel — dashboard hosting | ✅ live; security-boundary deployment `dpl_AUeRQZEDRxx1tfefCHKDLjRpiQ8W` is exact Careon commit `221a986`, Ready on `www.careonpulse.com` and all production aliases. The canonical public-app URL is `https://www.careonpulse.com`. Registry/handoff/device and TGC sync APIs fail closed and remain private/no-store; the authenticated Facturatie PDF route is `private, no-store` on success and every error/denial path. The server-side push-token key and TGC portal credential stay in their separate protected server/local-worker boundaries; neither value is exposed. |
| Hetzner VPS — comms plane | ✅ provisioned 9 Aug 2026: CPX22 `yaaz-comms-1` (nbg1, Ubuntu 24.04, firewall 22/80/443, Hetzner backups on), YAAZ live at `https://157-90-231-67.sslip.io` (temp host until the platform domain lands). G13 production fixtures were removed 21 Aug after a verified backup; `careon-demo` is disabled and the real TGC Space/identity bindings are preserved. **Coolify deferred — deviation from D11**: deploys run over SSH + `compose.yml`+`compose.tls.yml` (image's own Caddy terminates TLS); revisit Coolify when staging + operator self-service are needed |
| JaaS (8x8), Cloudflare R2, Vertex AI (Gemini) | 🔵 JaaS native bridge/build and server authorization/signing code are proven locally and remain dormant; a real account, AppID/key, approved processing region/DPA and live signed-device join acceptance do not exist. R2 backup publication, verification and exact-pair fetch code is production-deployed fail-closed, but the client-owned private EU bucket/token and lifecycle rules do not exist yet. Vertex is not created. |
| Apple / Google developer accounts (client-owned) | ⬜ blocked on legal-entity decision (§25 Q2) |

---

## 2. Timeline

### ✅ Done

- **Pulse dashboard built, hardened, and live** — full KPI screens, AI-assistent, org-scoped auth, production audit passed (24 Jul 2026), auth hardening landed (26 Jul 2026); role-based financial confidentiality added 28 Jul 2026 (members see no financial data anywhere — pages, widgets, data APIs, AI-assistent; org_admins unchanged; see RELEASE_GATES.md)
- **Platform design finalized** — v1.0 blueprint (26 Jul: HumHub + JaaS + Gemini single-app design, hosting and vendor research, JaaS pricing verified) → multi-module pivot → **v2.0 blueprint (27 Jul)**: shell + Supabase identity hub + module registry; decision log locked D1–D18
- **Umbrella docs added to this repo** — blueprint v2.0, AGENTS platform section, this status file

### 🔵 Where we are now

**22 Aug 2026 — public database privilege boundary hardened; Supabase Auth settings inventoried.** Careon `221a986` and live migrations `20260822235000_security_invoker_boundaries`/`20260822235500_revoke_legacy_facturatie_rpc` remove the advisor's public security-definer view and authenticated privileged-RPC findings. The agenda projection now invokes with caller rights while a schema-private function preserves exact organization filtering and financial redaction; live member/admin row counts and projection digests are unchanged. Invoice finalization/crediting uses a service-only replacement after both application and database revalidate the initiating administrator; `anon` and `authenticated` cannot execute it, and the legacy RPC is absent. Full CI/build are green (**1000/412/145/105/70/33/46**, 106 routes, zero npm audit findings). Vercel `dpl_AUeRQZEDRxx1tfefCHKDLjRpiQ8W` is exact `221a986`/Ready. Live readiness, Microsoft entry and anonymous PDF denial pass; signed-in Zairo Facturatie acceptance shows the admin action and correct empty state with zero browser warnings/errors. The remaining advisor INFO notices are intentional no-policy forced-RLS service tables. Read-only Auth inspection confirms email OTP expiry is `10800` seconds and leaked-password protection is off; applying `3600` plus protection-on remains an explicit account-access policy change.

**22 Aug 2026 — G10 server-authorized meeting foundation built dormant; gap in progress.** Sibling `humhub-meeting-modules` commit `32d4484` adds installable HumHub `meeting-core` **0.1.0** alongside the already-proven native bridge. Every participant token revalidates the enabled YAAZ account, exact `careon`/Supabase UUID binding, explicit session invitation and join window; a server-only minimum-4096-bit RSA key signs a 15-minute literal-room JaaS contract that omits e-mail and disables recording/transcription/livestreaming/file upload/outbound/SIP. Database checks independently enforce no recording, valid windows/status/roles and unique invitations. PHP lint, **46/46** pure checks, **13/13** rollback-only runtime checks, weak/strict key-permission health, guest redirect and signed-in dormant-page browser acceptance are green. The production platform, schema, navigation and shell are unchanged; no real account/key/token or media permission was introduced. Packaging/parity/backup, tenant/DPA/region and signed Android/iOS physical-call acceptance remain before activation.

**22 Aug 2026 — G17 off-site recovery code deployed fail-closed; gap in progress.** Platform `5a9d63c`/host `7498990` publishes a remote completion manifest only after the MariaDB and HumHub `/data` objects succeed, pins each object to local SHA-256/pair/kind metadata, and rechecks remote completeness/freshness from both backup and five-minute health. The exact-stamp recovery fetch refuses overwrites and unsafe output paths, validates content plus metadata and leaves restore behind the existing separate confirmation. The credential-free failure matrix is **18/18**. A fresh production DB/`data` pair and separate code archive preceded rollout; both timers and service results are green. Production deliberately reports `offsite=disabled`: no TGC-owned R2 account, private EU bucket, token or external write was invented. Remaining G17 work is client activation/lifecycle rules, an isolated fetch+restore, secondary invoice-storage backup, owned external alert delivery and secret rotation.

**22 Aug 2026 — G17 Facturatie Storage integrity/snapshot boundary live; gap in progress.** Careon `868341a` gives the separate Supabase Storage plane four validated archive-metadata constraints and a private bucket restricted to PDF/PNG and 25 MB. A service-role-only operator command recursively reconciles database references and live objects, then verifies exact byte count, MIME, file signature and SHA-256 without logging business content; object failures use only a short path digest. A guarded no-overwrite local snapshot is available only outside the repository with an explicit plaintext acknowledgement for an approved encrypted volume. The policy suite is **33/33**. Live preflight/postflight found zero partial metadata, zero expected objects and zero stored objects; the no-write production check returned `FACTURATIE_STORAGE_VERIFY=OK objects=0 bytes=0 bucket=private`. Vercel `dpl_8C48j6JDwfR18mnmHRihykvUppeV` built exact `868341a` Ready; liveness/readiness, Microsoft entry, anonymous PDF denial and the first warning/error scan are green. No content was copied. G17 still needs the client-owned encrypted secondary destination/schedule and a non-empty isolated restore drill before the Storage RPO is met.

**22 Aug 2026 — G07 TGC worker availability live; gap in progress.** Careon `925dd04` and live migration `20260822233000_tgc_worker_heartbeat` add a service-written, organization-scoped heartbeat at startup and every 30 seconds; the table is forced-RLS, authenticated users have read-only organization access, and only `service_role` can execute the empty-search-path RPC. Databron now shows **TGC-worker beschikbaar**, **niet bereikbaar** or **onbekend** while preserving safe queued requests. The exact Windows task `Careon TGC AI Import Worker` was restarted from its former idle/failing state and recurring live updates were observed. `cd7b350` makes the full TGC API boundary private/no-store. Focused queue verification is **46/46**, full CI and the 106-route build are green, and Vercel `dpl_7Hhxna5N6nDhNCSdn5n5SKPVzecK` is exact `cd7b350`/Ready. Authenticated Zairo production acceptance shows the available badge and API state with zero browser warnings/errors; no import was triggered. G07 remains `In progress` for a vendor API or centrally owned ingestion host, freshness SLA and external alert ownership.

**21 Aug 2026 — G01 employee onboarding and G15 offboarding closed end to end.** `PLATFORM_GAP_REGISTER.md` is the persistent source of truth for gaps G01–G17, consequences, acceptance criteria and evidence. Entra is authoritative for identity and explicit `Careon.User` eligibility; Careon remains authoritative for organisation roles and entitlements. Production Directie reads all 66 Office identities and marks exactly 3 eligible employees separately, so visibility never implies login permission. Zairo's existing-account Microsoft linking preserves `org_admin`; a disposable new tenant member proved automatic creation of exactly one Azure identity, one audited TGC `member` membership, one separate YAAZ account and no confidential/admin entitlements. The acceptance run also exposed an unsafe HumHub sticky-account link: platform `6c2c813` fixed it before rollout with `careon-sso` 1.1.0 subject/e-mail/token guards and `careon-m365` 0.4.2 ambiguity refusal. The controlled disablement then made Careon return `authed:false`, invalidated the existing YAAZ session, disabled only the disposable YAAZ identity and left Zairo active and Microsoft-connected. The test identities were cleaned up while lifecycle evidence was retained; a final reconciliation observed 66 identities and no actions. Gates are Careon **998/998** product, **412/412** production, **145/145** assistant, **102/102** runtime-hardening and **34/34** queue checks; YAAZ **10/10** SSO identity checks and **125/125** rollback-only Microsoft 365 checks. Production module source/runtime hashes match and all containers are healthy.

**21 Aug 2026 — G03 calendar-only Outlook writes live; final cancel acceptance pending.** Blueprint v2.5 amends D21 after the owner's integrated-calendar approval. Platform release `a9eb4f8` deploys `careon-m365` **0.5.0** with independent mail/calendar/file flags; only `M365_GRAPH_CALENDAR_WRITE_ENABLED=1` is active. The Graph app has tenant-admin consent for delegated `Calendars.ReadWrite`, while `Mail.Send` and `Files.ReadWrite.All` remain absent and disabled. Create/edit/cancel changes only title, location and timing, never bodies or attendees; edit/delete re-read and compare the current Graph ETag and send `If-Match`. Backups were captured before deployment, Compose and health are green, host/runtime hashes match, SSO remains **10/10**, and the rollback-only M365 verifier is **153/153**. Zairo's renewed consent and live disposable event proved create, overlay visibility, edit and refreshed visibility. The event is deliberately still present until the required action-time confirmation permits the final live cancellation and absence check; G03 remains `In progress` for that one acceptance step.

**21 Aug 2026 — G13 production demo data removed and closed.** Platform release `94b5304` adds an exact-signature, dry-run-first cleanup and fixes the stopped-container `/data` restore path. A complete local DB + `/data` destructive cleanup/restore rehearsal passed before production. Verified production DB, `/data`, code and environment backups were then captured; preflight matched the known fixtures exactly. The transaction removed the five fictional identities, showcase feed, local demo agenda and fixture conversations while preserving the TGC Space, five real memberships, five Careon OIDC bindings and Zairo's Microsoft connection. Independent SQL, rebuilt search/cache and browser acceptance agree: zero fixture users/content/messages, empty TGC feed, only five real platform accounts and no seeded local calendar titles. `careon-demo` is disabled. The still-visible disposable Outlook event belongs only to the separate G03 cancellation acceptance.

**21 Aug 2026 — G17 host operations active; gap in progress.** Platform host release `8bc6003` is deployed with tracked systemd timers: every night it creates and verifies a mode-600 MariaDB + HumHub `/data` pair, and every five minutes it checks containers, real DB/Redis queries, internal/public TLS, scheduler, the configured queue-worker count, M365 health and backup age. Local positive/negative tests and the first production service runs are green; the first timer backup is `db-20260821-181306.sql.gz` + `humhub-data-20260821-181306.tar.gz`. Careon's formerly failing readiness probe identified the missing dedicated assistant safety salt; a new 256-bit sensitive Production/Preview value and redeploy made all readiness checks true with no early warnings/errors. Platform `1f292fd` adds hourly external GitHub smoke. Remaining G17 work is off-site R2, secondary invoice backup, assigned pager/e-mail delivery on the final domain, an isolated production-like restore and scheduled Entra-secret rotation.

**21 Aug 2026 — G12 Facturatie clean production baseline; gap in progress.** A data-minimised Supabase inventory found no TGC sender settings or contacts, five never-issued empty-recipient development concepts, and no issued invoices, PDF objects or mail attempts. An exact count/SHA-256/semantic-guarded transaction removed only those five rows and recorded a metadata-only operations audit event; postconditions are a clean zero-row TGC invoice administration and empty private `facturen` bucket. Live migration `20260821182849_facturatie_fk_indexes` closes the two missing-FK-index advisories, with source migration `20260821213000_facturatie_fk_indexes.sql` and a runtime verification guard. Commit `c1f4dac` is live as Vercel `dpl_ThUvnG56iWcWmFzik52NZnXeNhey`; full CI, dependency audit, production build, readiness/route smoke and the first warning/error log scan are green. G12 now waits only on real TGC legal/company/contact data, the Resend DPA plus authenticated sender domain, and one controlled invoice lifecycle acceptance.

**21 Aug 2026 — G08 unified web navigation closed.** YAAZ runtime release `5523fde` upgrades `careon-sso` to 1.2.0 and adds a persistent signed-in **Modules** entry back to the protected Careon launcher; `81fc34d` adds a production-refusing local authenticated acceptance script. The target is restricted to HTTPS `/modules`, cross-origin PJAX is disabled and invalid configuration hides the entry. A verified production backup preceded rollout; **20/20** checks, authenticated local render acceptance, identical production source/runtime hashes and the full live host health gate are green. Together with Careon's existing launcher, dashboard user-menu switcher, Facturatie return link and Careon → YAAZ OIDC handoff, this closes the web navigation loop. The future native shell remains G09.

**22 Aug 2026 — G09 one-time WebView SSO, reproducible Android build, dormant opt-in push receiver and governed native file bridge are implemented; mobile gap in progress.** The shell's handoff increment `516e2db` requests an exact entitled-module handoff, refreshes/retries once on authentication failure and POSTs the raw capability directly from memory into a guarded WebView. Careon `a2f9b7f` and Supabase migrations `20260822210000`/`20260822213000` implement the 60-second single-use digest-only handoff, while YAAZ `careon-sso` **1.3.1** preserves the safe deep link and removes ambient secret-bearing exception context. Live Zairo acceptance lands in `/microsoft-365` without a second login; replay is `401` and a foreign organization is refused. Shell `beee34b` established the reproducible OpenJDK 17/API 36 build; `9e3ef5c` adds explicit opt-in FCM initialization, secure installation identity, registration/token-refresh/sign-out revocation, inert foreground text and strict title + entitled-deep-link parsing. Careon `3414dfb` plus live migration `20260822230000` add the exact-client-bound device API and service-only forced-RLS registry with AES-256-GCM ciphertext/HMAC digest storage and cross-account reassignment revocation. Shell `d496ca6` now handles approved Android uploads through the OS picker and handles generated/archived PDF or CSV save/share on Android and iOS without exposing tokens or broad device storage. Careon `9b2b5f3` supplies the exact user-agent/channel bridge while retaining browser downloads; `a914812` makes every Facturatie PDF response `private, no-store` and adds the CI regression guard. Full Careon CI is green (**1000** product, **412** production, **145** assistant, **103** runtime, **70** mobile, **34** queue, zero npm vulnerabilities), its 106-route build succeeds, and Flutter analyze + **36/36** tests plus debug/release compilation pass. The current 52,909,354-byte unsigned release APK is SHA-256 `42558815541EF5F4D63498E08EE0558B647D53ECD79C3D4896F921B9E6E2B7B8`; backup/cleartext and Firebase Messaging/Analytics auto-init are off, and its only functional permissions remain notification/FCM/network/wakelock. Vercel `dpl_outqmGNSX2yU3uBunNDQVY5mKkLm` is exact `a914812`/Ready. Readiness is fully green; anonymous module and PDF probes return `401` with `private, no-store`, the correct `Vary` boundary and `nosniff`. G09 remains `In progress` for TGC-owned Firebase/APNs configuration, a least-privilege sender, real Android/iOS push/file/account-switch/offboarding acceptance, client signing/privacy metadata and store/MDM delivery.

**22 Aug 2026 — YAAZ exception-log secret context removed.** Callback diagnosis revealed that the official HumHub container exposes deployment variables through PHP `$_SERVER`, while Yii's default log targets copy `_SERVER` and the other ambient request globals into every exception record. Platform `6b1f44c` upgrades `careon-sso` to **1.3.1** and clears `logVars` before request handling without suppressing the exception or stack. The pure suite is **38/38**; the active runtime reports two hardened targets, and the five-minute host gate now checks both activation and exact source/runtime parity. A fresh verified backup preceded production. A harmless invalid OAuth callback then appended a normal 2,163-byte error record with zero markers for Graph client/encryption secrets, lifecycle/directory keys, database credentials or cookies. Host health, backup freshness and module hash `8a063cf2...c899` are green. Existing server logs and backups remain confidential operational material; no external disclosure was observed.

**21 Aug 2026 — G02 Outlook message detail deployed fail-closed; gap in progress.** YAAZ runtime `a67d04a` upgrades `careon-m365` to 0.6.0 with a separate delegated `Mail.Read` content gate. The production flag remains off, leaving current `Mail.ReadBasic` consent and inbox metadata untouched. Once activated, a subject opens live text-only content inside YAAZ with no persistence, active HTML, remote images/tracking pixels, MIME, attachment bytes, internet headers or read-state change; `Mail.Send` remains independent/off. The rollback-only suite is **177/177**, production source/runtime hashes match and a verified backup preceded deployment. Rollout also exposed a stale local capability set before acceptance; the authoritative server environment restored calendar writes, and `3a79cfa` now makes five-minute health fail if any of 10 accepted non-secret flags/URLs drift. Production is **10/10**, `calendar=on`, all mail/file writes off and healthy. Remaining G02 work is explicit `Mail.Read` permission/consent, one real employee reconnect and live body-display acceptance, then separately approved reply/send and attachment behavior.

**21 Aug 2026 — G05 SharePoint folder navigation live; gap in progress.** YAAZ runtime `c0991bf` upgrades `careon-m365` to 0.7.0. Employees can now navigate the configured canonical library and subfolders inside YAAZ with bounded Graph paging, while only Microsoft metadata crosses the service boundary and each user's delegated SharePoint ACL remains authoritative. Crafted folder IDs, cross-host next links and configured-subtree escapes fail closed; file content/download URLs are never requested or persisted. The suite is **204/204**. A fresh verified backup preceded production; source/runtime hashes match (`f0cac1b6...fe14e`), capabilities are **10/10** and host health is green. Zairo's signed-in Careon → YAAZ acceptance renders `TGC SharePoint`, the exact `All Company/Gedeelde documenten` handoff and a polished connected-empty state with zero browser errors. `98fa8ab` additionally hardens Linux release line endings after the capability gate caught a CRLF-only policy mismatch. G05 still needs approved company content/governance and a separate file-write/upload consent decision.

**21 Aug 2026 — G04 Teams navigation repaired; gap in progress.** YAAZ runtime `4572cb4` upgrades `careon-m365` to 0.8.0. The overview now makes one supported `/me/joinedTeams` call; opening a team rechecks delegated membership and loads only that team's accessible channel metadata. This replaces all placeholder links with official HTTPS Teams handoffs and removes the throttling-prone burst that produced a live `TooManyRequests` banner. No channel e-mail, posts, chats, members or message bodies are requested and no permission changed. The suite is **227/227**, SSO is **20/20**, a fresh verified backup preceded deployment, source/runtime hashes match (`5dfc3409...a14b`), capabilities are **10/10** and host health is green. Zairo's live Backoffice flow renders the exact accessible channel and Teams deep link with no new throttle logs or YAAZ module console errors. G04 still needs an explicit product/privacy decision before any in-YAAZ messages, chat or meeting workflow.

**21 Aug 2026 — G11 governed Space rollout boundary live; gap in progress.** YAAZ runtime `9dd4ab7` adds console-only `careon-spaces` 1.0.0. It inventories the real Space/ACL state and validates a private TGC-IT-approved Team mapping before calculating settings, exact member, owner, optional-module and retention drift. Output contains only aggregate identity counts and plan hashes. The command surface exposes only `inventory` and `plan`; there is deliberately no apply/create/delete/membership action. Its policy/redaction suite is **36/36**, while M365 remains **227/227** and SSO **20/20**. A fresh verified backup preceded production. Module source/runtime hashes match (`82aa87ce...0485`), capabilities remain **10/10**, the host and both systemd timers are green, and production remained exactly one `TGC Groep` Space with 5 active members, 1 admin, no pending membership and no content. The seven Teams visible to Zairo are candidates, not proof of complete organization membership. G11 now waits on TGC IT's approved Team → Space → owner → exact-member → retention manifest before a separately reviewed transactional apply release.

**21 Aug 2026 — G14 Dutch presentation increment live; gap in progress.** Careon release `c0bd44e` gives the YAAZ launcher tile a responsive CSS wordmark and replaces its mixed English description with `nieuws, gesprekken, ruimtes en documenten`; Vercel deployment `dpl_3oe5gTNc39dWgWwDNnC5pFMuiQZ4` built the exact commit across all 106 routes and is `Ready` on every production alias with no first-hour error/fatal logs. YAAZ host `87d830d` upgrades `careon-ui` to 1.1.0, translating the real HTTPS login title to `Inloggen`, and changes `Microsoft ACL` to `Microsoft-toegang`. Pure UI checks are **3/3**, the existing M365/SSO/Space suites remain **227/227**, **20/20** and **36/36**, the UI source/runtime hash matches (`afe8cf25...71829`), capabilities remain **10/10**, severe YAAZ logs are zero and full host health is green. Fresh verified backup pairs `20260821-201708` and `20260821-201931` preceded the two production steps; the real domain and a licensed/supported HumHub white-label decision remain external G14 work.

**Deliverable 1 (the Pulse dashboard) is complete — both operator actions are closed.** A multi-agent audit on 29 Jul 2026 (`docs/AUDIT_2026-07-29.md`) produced 40 confirmed findings; all code-fixable ones are fixed and verified (see RELEASE_GATES.md, "Auditronde 29-07-2026"). The two items that were not closable by code:

1. ✅ **Done (29 Jul 2026): migrations `0015_financieel_rls.sql` and `0016_login_account_throttle.sql` are applied to careon-zsg** and functionally verified in the database — a TGC `member` now reads zero rows from the financial tables and only redacted agenda data, while `org_admin` is unaffected. A same-day schema-drift audit (`docs/SQL_DRIFT_2026-07-29.md`) confirms every other migration is applied, RLS is enabled on all 17 tables, and there is no reverse drift. **Deploy the matching code** — the agenda route reads the new view (it falls back to the base table if the view were missing, so ordering is safe).
2. ✅ **Done (9 Aug 2026): Supabase DPA settled** — confirmed by the owner on 9 Aug 2026 (see `PRODUCTION_MODE.md` §"Vereisten vóór publieke hosting van echte data"). This was the last remaining blocker for deliverable 1.

On the platform side, design is closed and **Phase 0 — Audit & Foundations (§22) started on 30 Jul 2026**: `platform-deploy` is scaffolded (see the sibling-repo table above) with the pinned compatibility matrix (`VERSIONS.md`) and the Spike-A plan — and, same day, **the comms stack runs locally**: Docker CE installed in WSL, `docker compose` boots HumHub 1.18.4 + MariaDB 11.8 + Redis 8.0, `scripts/provision.sh` completes non-interactively, the CareonTheme (palette ported from the dashboard's careon design tokens) compiles and is active, the Messenger (`mail` 3.4.3) module is enabled, and login → dashboard is verified end-to-end at http://localhost. The repo is on GitHub since 9 Aug 2026 (`Mbongaa/platform-deploy`).

**31 Jul 2026 — YAAZ demo-ready (historical; the production fixtures were removed under G13 on 21 Aug):** (1) role mapping live (see Spike A below); (2) the organisation network "TGC Groep" exists — created by the org admin through the real space-create UI (space creation is Administrators-only, verified 200/403 per role) — with `auto_add_new_members` on and a seeded Dutch demo timeline (11 posts, 9 comments, 22 likes, 5 fictional practitioner accounts named after the dashboard's behandelaren, initials avatars; seeder = `careon-demo` console module in `platform-deploy/humhub/modules-custom/`); (3) **full CareonPulse rebrand 1:1 with the dashboard's careon theme**: navy-glass look as the single theme mode (CE 1.18 has no runtime dark-mode switch, and the dashboard's careon mode is fixed-dark anyway), Geist self-hosted, gradient logo lockup + favicon + login background generated from the exact handoff SVG/tokens (`platform-deploy/humhub/branding/`, installed by `scripts/sync-humhub-assets.sh`, which now also flushes the `theme.var.*` settings that would otherwise override `variables.scss`), default language `nl`, timezone Europe/Amsterdam. "Powered by HumHub" remains (removal = PE whitelabel module, quote needed).

**9 Aug 2026 — YAAZ public preview live** at `https://157-90-231-67.sslip.io` (Hetzner CPX22 `yaaz-comms-1`, nbg1). The verified local instance was migrated wholesale (DB dump + uploads), so the TGC Groep network, all accounts, the CareonTheme rebrand and NL settings are byte-identical to the 31 Jul demo state. SSO chain verified end-to-end against production: HumHub → Supabase authorize (PKCE, new redirect URI registered on the `careon` client) → consent on `https://careonpulse.vercel.app` (Supabase `site_url` fixed from its dev value; `/oauth/consent` confirmed deployed). Mercure hairpin passes, queue workers + scheduler run, Messenger pinned at 3.4.3 (marketplace served 3.3.12 — replaced to match the imported migrations). Ops gotcha recorded in `platform-deploy/docs/coolify-deploy.md`: `/data/config/common.php` (authclient class-swap + secrets) is NOT in the repo and must be copied per environment. Remaining for the tile: set `NEXT_PUBLIC_YAAZ_URL` in Vercel; later: real domain, R2 backups + restore drill before real rollout.

Phase-0 research produced **five corrections to blueprint §4/§13/§19 — each proposed, none applied to the blueprint yet** (full findings + revised spike checklist in `platform-deploy/docs/identity-spike.md`; topology also in `platform-deploy/VERSIONS.md`):

1. The **Supabase OAuth 2.1 server's GA status is ambiguous** (re-checked live 30 Jul 2026): the docs guide no longer carries beta wording, but Supabase's feature-status page still labels it "Public Beta" (beta opened Nov 2025; §4's "left beta late 2025" remains unconfirmed either way). Confirm GA/SLA/pricing directly during Spike A.
2. **We must build the consent screen ourselves** (a route in our own app; Supabase ships no consent UI), and the identity project must first migrate to **asymmetric JWT signing keys** — its own change window, blast radius beyond OAuth.
3. **No OIDC logout/revocation/introspection exists**: single logout must be an application-level fan-out, and §4 "deactivation locks the user out at next token refresh" holds only with a short `jwt_exp` (300–900 s). §22 Phase-0 "logout" and Phase-1 "deactivation locks a test account out of everything" acceptance wording need the same nuance.
4. **Custom claims cannot go in ID tokens** (closed struct) and `app_metadata` is not exposed by userinfo — §4's "ID tokens carry … role and tile entitlements" must become access-token claims or the entitlements-endpoint variant; never mirror roles into user-writable `user_metadata`.
5. **§13/§19 comms topology is stale**: the official HumHub image is a single FrankenPHP container (supervisord runs web + cron + queue workers) — no separate nginx/worker/cron services; D9's substance is unchanged. Also §19's `/health` + `/health/queue` endpoints don't exist in HumHub — recorded as a Phase-1 build task.

The shell foundation exists in its own sibling Git repository (current documentation HEAD `7460991`; runtime-bearing file bridge `d496ca6`). The meeting sibling is at `32d4484` with the isolated bridge/build spike and installable disabled-by-default `meeting-core` server foundation, but no deployed production package/config or real JaaS tenant. The longest external clock remains which legal entity owns the Apple/Google developer accounts (§25 Q2), because signing and store enrollment cannot be completed from engineering code alone.

Known residual, deliberately deferred to the second organisation's onboarding: the app's customer identity ("TGC Groep") is still hardcoded in audited page copy and demo constants. Public/unauthenticated surfaces (meta, OpenGraph, login page) no longer leak it, but making branding session-driven changes audited copy and must be done together with an update of the audit documents.

**9 Aug 2026 — facturatie module BUILT (D19).** The client requested an invoicing tool as a separate admin-only module; the owner answered the 21 inventory questions on logic-based defaults and instructed to build in parallel with Phase 0 (recorded exception in blueprint §22). Implemented same day per `agent-handoff/15-facturatie.md`: standalone module `/facturatie` with its own shell + menu (list, editor with live blob-PDF preview via `@react-pdf/renderer`, contacts incl. employee union, settings), migration `0020` (four tables, RLS on `app.mag_facturatie_zien`, atomic number+status RPC, immutability triggers, concept-prune RPC, private Storage bucket `facturen`), four-layer admin gating, full client-side demo path (Playwright runs demo-only), and two deliberate CSP changes (`frame-src 'self' blob:`, `'wasm-unsafe-eval'`). Gates: check/typecheck clean, `verify:careon` 969, `verify:runtime` 60, facturatie e2e subset green; docs updated (blueprint v2.1 + D19, RELEASE_GATES, PRODUCTION_MODE, DISASTER_RECOVERY, AGENTS). Same day, on client request: settings became a **template library** (multiple sender profiles per organization, per-invoice template choice, per-template numbering freeze and logo) and the PDF layout was rebuilt 1:1 to the client's imported "Factuur Careon Group" design (claude.ai/design, brand-gradient top bar/wordmark/TOTAAL band; built-in `careongroup` template with bundled logo). **Phase B (e-mail dispatch) is deliberately not built** — provider choice (EU-sovereign preferred) + DPA first *(superseded 13 Aug 2026 — built fail-closed, see below)*. Real company data (KvK/IBAN/address/logo) is runtime settings the admin fills in; client validation of the inventory answers still advised.

**13 Aug 2026 — M365 direction confirmed (D20, blueprint → v2.2) + YAAZ modules live.** The owner confirmed **hybrid authentication**: Entra ID federates into the Supabase hub as upstream provider, "Inloggen met Microsoft" becomes the primary path for TGC employees, e-mail/password stays for platform administration, demo/e2e, and break-glass; per-org enforcement is a later explicit phase. Recorded as **D20** in blueprint v2.2 (+ §25 questions 13–15; Q9 answered for TGC). Track B (implementation) blocks on the TGC-IT app registration — the draft request message is ready in `agent-handoff/16-office365-yaaz-modules.md` §8. Track A executed the same day: **calendar 1.8.16, polls 1.4.5, tasks 1.9.6** installed and enabled on local + production YAAZ (marketplace versions matched — no parity fix needed), versions pinned in `VERSIONS.md`; demo agenda seeded on both environments (`yii careon-demo/agenda`: 5 events congruent with the demo posts incl. 4 RSVPs and a weekly recurring intervisie; `agenda-check` proves recurrence expansion; ICS feed verified valid on both, sabre/vobject clean); CareonTheme extended with module coverage (FullCalendar/tasks/polls hard-coded light styles overridden to navy-glass — commit `9d41193`). A platform-wide **Office 365 integration assessment** (per module: config vs off-the-shelf vs custom vs advise-against, adversarially verified against primary sources) is recorded in handoff 16 §10 — headline: everything the client reasonably means by "integrate with Office 365" is feasible; custom build is limited to four small, contained pieces; two-way calendar sync, Teams meeting interop, and SCIM are explicitly not viable/not worth it. Ops notes: `/opt/platform-deploy` on the server is a file copy, not a git checkout; calendar ICS feed tokens never expire (module bug — `jwtExpiration`/`jwtExpire` mismatch), revocation = rotate `jwtKey`.

**20 Aug 2026 — Microsoft 365 delivery built and initially deployed fail-closed (D20 + D21, blueprint → v2.3).** The dashboard received the Supabase Azure PKCE login with existing-account/org gating; YAAZ received custom module `careon-m365` **0.2.0** for read-only Outlook, calendar overlay, Teams/channels, documents and Microsoft Search, with encrypted delegated tokens and separately gated writes. The rollback-only verifier covers **99** OAuth/PKCE, exact-account, encryption/rotation, Graph-error, URL/bearer and Calendar-provider boundaries. YAAZ was deployed with backup `/opt/platform-deploy/backups/code-20260820T190835Z-m365-0.2.0`, matching source/host/runtime manifest hash `b80ae83e286788d672359821eb9076a6c59de341e929beb057c5440b3495ed62`, current migration, healthy Compose and a clean zero-connection baseline. This recoverable pre-activation state was superseded later the same day by the tenant activation below. Exact permission matrix, runbook and acceptance evidence: `agent-handoff/17-microsoft365-yaaz-deliverable.md`.

**20 Aug 2026 — Microsoft 365 tenant activation and live acceptance complete; SharePoint source closed 21 Aug.** In TGC tenant `397ebf46-04bb-4721-981f-e33d68849208`, the separate login app (`4b3481ee-d4e2-49a3-8ece-252f2ce8c957`) and YAAZ Graph app (`c19cff7d-f50f-465b-9ba9-68a630b37e61`) are registered single-tenant. Supabase Azure is enabled on the exact tenant URL; the production feature flag is on; an existing TGC user completed Microsoft PKCE login and retained the same Careon organisation gate. Supabase shows one Azure identity and clean auth events. Tenant admin consent is granted for the seven delegated read permissions in handoff 17; one YAAZ user connected successfully. Live Graph acceptance rendered Outlook inbox metadata, the 14-day calendar, joined Teams/channels, documents and ACL-aware Search; the native Calendar rendered local YAAZ and read-only Outlook events together. Access and refresh tokens are AES-256-GCM ciphertext in MariaDB and no Microsoft bearer/refresh token or client secret appeared in browser HTML or filtered server logs. The production containers and `yii careon-m365/health` are green, with `write=uit`; Vercel's one-hour error scan is empty. Both active client secrets expire **16 February 2027**; rotate them before that date. The obsolete unused first login secret was removed after activation. On 21 Aug the new read-only `careon-m365/discover` command inventoried 19 accessible SharePoint/Teams libraries without file contents. The root `TGC` library contained only Facturatie, department libraries were rejected as too narrow, and the clean stable `All Company` library at `/sites/allcompany/Gedeelde documenten` was configured as `M365_GRAPH_SHARED_DRIVE_ID` with no folder restriction. Live YAAZ acceptance shows the correct empty shared-library state and no personal OneDrive fallback or Graph error.

**D20/D22 deployment clarification — updated 21 Aug 2026:** Careon authentication release `a47399e` and platform release `9dd4ab7` are pushed; Microsoft is the deployed primary employee sign-in and the paired YAAZ runtime runs `careon-sso` 1.2.0, `careon-m365` 0.8.0 and read-only `careon-spaces` 1.0.0. Live Directie reports the clean 66/3/3/0 state for Microsoft identities / eligible / active Careon / eligible first-login pending. First launch, exact-account isolation, session revocation and the authorized lifecycle path pass in production. G13 fixtures are removed and `careon-demo` is disabled. G16 remains closed by committing each reviewed source change before treating its production deployment as final.

**13 Aug 2026 — facturatie gap-audit + fixronde.** A 23-agent audit of the built module against its blueprint (`agent-handoff/15-facturatie.md`) confirmed 268 implemented requirements and found 12 real gaps; all twelve were closed the same day — among them the art. 35a sub h basis line in the totals block, full validation before a credit invoice is inserted, server-side "Te laat"/"Openstaand" filtering with correct pagination, and a complete contact form (all fields editable). Gates after the round: `verify:careon` **989/0**, `verify:runtime` **72/0**, typecheck + Biome clean, contacten-e2e green; a live smoke suite for the server path (`verify:facturatie:live` against careon-zsg) now exists — see `RELEASE_GATES.md` and handoff 15 §0.4.

**13 Aug 2026 — facturatie phase B (e-mail) built.** Sending an issued invoice by e-mail now exists end to end: migration `0021_careon_facturatie_mail.sql` (applied to careon-zsg and verified against `pg_policy`/`pg_constraint`/`pg_proc`) adds `careon_facturatie_maillog` — one row per send attempt, RLS select-only via `app.mag_facturatie_zien`, writes service-role only, excluded from every prune (7-year administration) — plus the `mail` quota scope in `careon_consume_assistant_quota` (CHECK constraint *and* function allowlist). Provider is **Resend** (owner decision 13 Aug 2026, deviating from the EU-sovereign V17 proposal — consequences recorded in the D19 amendment; platform-wide per D19); the mail body carries only invoice number, total, due date and sender name/IBAN (enforced in `verify:careon`), the archived PDF rides along as attachment, and the audit event `facturatie.factuur.send` deliberately omits the e-mail address. **Fail-closed until the DPA**: while `CAREON_MAIL_RESEND_API_KEY` / `_AFZENDER_EMAIL` / `_AFZENDER_NAAM` are empty the route answers 503, so there is no send risk before the processing agreement. Deliberate deltas from the phase-B design: synchronous sending with no queue worker (the cron only marks stuck attempts as failed — automatic retry would risk double-sending a legal document) and a reserved `gebounced` status without a bounce webhook. Gates: `verify:careon` **995/0**, `verify:runtime` **80/0**, typecheck + Biome clean, e2e demo send test, live smoke extended with the fail-closed 503 check and maillog-RLS probes (it never sends real mail). Go-live checklist in `PRODUCTION_MODE.md`; detail in handoff 15 §0.5.

### 🔵 Current — Phase 0: Audit & Foundations

1. 🔵 Create the sibling repositories — `platform-deploy` is live on GitHub; `careonpulse-shell` is committed locally at `7460991`; `humhub-meeting-modules` is committed locally at `32d4484`. Publish either local repository only after ownership/visibility is explicit.
2. ✅ Supabase **OAuth 2.1 server mode enabled** on careon-zsg; ES256 signing active; HumHub confidential client and consent screen live. The mobile shell public client is registered with no secret, exact app callback and PKCE-compatible grants; Careon's client-bound module-registry API is live. A signed-device system-browser → app-callback acceptance still belongs to G09 before pilot.
3. **Spike A — identity (gate):** 🔵 largely passed 31 Jul 2026 — HumHub "Inloggen met Careon" wired (core OpenIdConnect, auto-provisioning open); real-browser first login and repeat visit both pass from the YAAZ launcher tile without a login, registration, or consent click (repeat verified after ending only the HumHub session, so the existing-grant path was exercised); Keycloak fallback (§4) not needed so far. **Role mapping is DONE (31 Jul 2026, later same day):** migration `0019_careon_access_token_hook.sql` puts a namespaced `careon` claim (`org_id`/`org_slug`/`org_role`/`superadmin`, read live from `organization_members`/`platform_admins`) in every access token via the GoTrue custom-access-token hook (SECURITY DEFINER; `lock_timeout` guard because `WHEN OTHERS` does not catch `query_canceled` — a lock wait would otherwise fail ALL token issuance), and the HumHub module `careon-sso` (`platform-deploy/humhub/modules-custom/careon-sso/`, subclassing `OpenIdConnect`) verifies the token against the hub's JWKS on every login and syncs Administrators-group membership both ways (org-bound: `org_admin` counts only for the installation's own org; superadmin is platform-wide; missing/invalid claim = no change; last-admin demotion refused). E2E-verified headlessly: promotion on first login, member stays member, demotion after role change, plus name sync (`SyncAttributes` from `user_metadata.full_name` — the id-token `name` falls back to the e-mail address and silently breaks auto-provisioning on HumHub's required 20-char firstname). Remaining: app-level logout fan-out, `jwt_exp` decision. Detail: `platform-deploy/docs/identity-spike.md`
4. **Spike B — meetings (gate):** 🔵 isolated WebView ↔ native contract/build plus the disabled HumHub authorization/signing foundation pass at `32d4484`; production packaging, real JaaS configuration, production-shell bridge and signed physical Android/iOS join/interruption/audio/permission acceptance still gate activation
5. ✅ Hetzner provisioned and YAAZ production live; direct-TLS Compose is the recorded operational path and Coolify is deliberately deferred until staging/operator self-service justify it
6. Decide the developer-account legal entity and **start Apple/Google enrollment immediately**
7. Local dev prerequisites on the dev machine: install Docker (WSL Debian, commands in `platform-deploy/docs/local-dev.md`) and authenticate `gh`
8. ✅ **M365 read-only activation:** D20/D21 are live and accepted; the canonical SharePoint library is selected. ✅ **D22 employee rollout:** the full 66-identity Directie inventory, eligible existing-account linking, brand-new default-`member` JIT creation, separate YAAZ first launch, exact-account isolation and controlled cross-plane offboarding are production-accepted

**Phase 0 acceptance:** both spikes pass (or fallback invoked); pinned versions install cleanly end to end; push-to-deploy works on both planes.

### 🔵 Phase 1 — Shell + Base Platform (§22)

Shell app foundation is built: native login, secure refresh, launcher + server-driven registry, current role entitlement, deep links, guarded WebViews, one-time Pulse/YAAZ session handoff, branded Android/iOS projects, reproducible Android builds, a dormant explicit-opt-in push receiver/device registry and governed native file handlers. Remaining: client-owned Firebase/APNs plus trusted sender, physical-device push/file acceptance, client signing/store submission, governed Spaces and the remaining operations/restore gates.

**Acceptance:** all employees onboarded via the identity hub; SSO into both modules on web and in the app; tiles match entitlements with server-side enforcement; deactivation locks a test account out of everything; push + deep links on iOS and Android; Space isolation; restore drill passed; both apps approved.

### ⬜ Phase 2 — Calling module (§22)

`meeting-core`, JaaS web embed, JWTs on the Supabase `sub`, invitations, native Jitsi screen in the shell, webhooks, permissions, shell update release. Scope stays push → tap → join (D5).

### ⬜ Phase 3 — Recording & AI module (§22)

`meeting-recordings` + R2 archive, pipeline jobs, `meeting-intelligence` (Gemini, Vertex EU), report review/approval, usage metering, retention automation — with healthcare access defaults: recordings, transcripts, and reports visible only to organizer + administrators until explicitly shared (§12, §18).

---

## 3. Open items needing a human decision

Full list: blueprint §25. Currently most urgent:

1. Developer-account legal entity (Careon entity vs TGC Groep) — blocks store enrollment
2. Platform domain
3. Recording-consent notice wording (client's counsel; healthcare context) — needed before Phase 3, drafted best during Phase 1
4. Comms-module branding: removing the "Powered by HumHub" footer/email links requires the HumHub **Professional Edition** `whitelabel` module (recurring cost, price via HumHub's configurator) — affects the §21 recurring structure; also confirm the module's client-facing product name (launcher tile currently says "YAAZ")
5. Facturatie e-mail go-live: **DPA incl. SCCs with Resend** (owner; US provider — owner decision 13 Aug, D19 amendment) + **sender domain incl. DKIM/SPF** (waits on the platform-domain decision, item 2), then the three `CAREON_MAIL_` keys in Vercel — the build is ready and fail-closed until then (13 Aug 2026, handoff 15 §0.5; checklist in `PRODUCTION_MODE.md`). Per D19 Resend is the platform-wide choice, shared with HumHub's `SMTP_*` in `platform-deploy` on the same sender domain
6. Facturatie **UBL/Peppol** (client answer V7): only needed once municipalities/government bodies are invoiced directly — today iWmo/iJw message traffic runs outside the module. The EN 16931 column semantics keep that door cheap; decide when a public-sector recipient actually appears
7. Microsoft 365 operations: assign a content owner and place the first approved shared documents in `All Company`; rotate both active client secrets before **16 Feb 2027**. Calendar-only `Calendars.ReadWrite` is approved and active; keep `Mail.Send` and `Files.ReadWrite.All` disabled unless separately approved (handoff 17 §§4–8)
