# Careon Pulse / YAAZ — Platform Gap Register

**Last updated:** 22 August 2026
**Status:** Active delivery register
**Authoritative architecture:** [`PLATFORM_BLUEPRINT.md`](./PLATFORM_BLUEPRINT.md)
**Live milestone status:** [`PROJECT_STATUS.md`](./PROJECT_STATUS.md)

This register preserves the product-readiness audit outside any single chat and turns it into a cross-session delivery backlog. It tracks the remaining distance between the current platform and the target: **employees can perform their normal work through Careon Pulse / YAAZ without navigating across disconnected systems**.

## Working rules

1. Every future implementation session reads the blueprint, project status and this register before changing platform behavior.
2. Confirmed blueprint decisions are not changed silently. A proposed change records its security, scope and operational consequences before implementation.
3. A gap becomes `Done` only after code/configuration, production deployment, role/security checks, failure-state checks and user-facing acceptance are evidenced.
4. Counts in the `Current reality` column are point-in-time observations and must be refreshed during acceptance.
5. Least privilege remains mandatory. Being present in a customer directory proves identity/eligibility; it does not automatically grant administrative roles or access to confidential modules.

Status legend: `Open` · `Proposed` · `In progress` · `Blocked externally` · `Done`

## Active goal

Close and track the Careon Pulse/YAAZ professional-platform gaps across sessions, beginning with secure Entra-driven employee onboarding and then resolving each documented product, rollout, Microsoft 365, EPD, navigation, mobile, meetings, branding, offboarding, source-control, backup and operational-readiness gap with verified production-ready changes.

## Gap inventory

| ID | Area | Current reality (22 Aug 2026) | Consequence | Target outcome | Priority | Status |
|---|---|---|---|---|---|---|
| G01 | Employee onboarding | Production Directie reconciles all 66 Entra identities and separately marks the 3 users assigned to `Careon.User`. Microsoft is the primary employee login for the whole dashboard. Existing-account linking and a disposable employee's complete first-launch path were production-accepted: Entra eligibility → one Supabase identity → one TGC `member` membership → a separate YAAZ identity. Assignment-required, tenant/member/verified-email guards, atomic JIT tests and the controlled offboarding run cover the denied and duplicate paths. | Ordinary eligible employees no longer need a Careon invitation or password. Entra controls eligibility while Careon roles and confidential module access remain independently governed. | Entra-gated, audited employee provisioning with safe default access, clear role ownership and reliable offboarding. | P0 | Done |
| G02 | Outlook mail | Production remains unchanged on YAAZ `careon-m365` 0.8.0 and delegated `Mail.ReadBasic`; the deployed v0.6 inert-text message-detail gate is still off. The earlier G02 source branch remains preserved at `c0a0e80`, and its reply/attachment increments are now reconciled with G04/G05 in pushed branch `codex/m365-workplace-dormant` at `19f629e` as module 0.12. With both separately accepted content/send gates, a selected message can receive one bounded plain-text direct reply; an independent attachment flag reuses `Mail.Read` but permits only revalidated non-inline PDF/Office/text/CSV/JPEG/PNG downloads up to 10 MB, max 25 listed, with no persistence and forced no-store/nosniff delivery. The integrated rollback-only suite is **380/380**; all new production flags remain `0` and no permission, consent or employee token changed. | Employees still need Outlook for message bodies, attachments and replies today. Text-only content needs the pending exact `Mail.Read` confirmation/reconnect/acceptance; attachments then need a separate privacy acceptance despite sharing that scope, while send/reply separately needs `Mail.Send` consent and disposable-mail acceptance. | Activate the deliberately scoped content, download-only attachment and send/reply surfaces in separate reviewed phases; keep reply-all, forward, drafts, outgoing attachments, HTML, read-state mutation and broader mailbox writes out unless independently justified. | P1 | In progress |
| G03 | Outlook calendar | YAAZ `careon-m365` 0.5.0 is live with calendar-only delegated writes. Employees can create Outlook events and open the live overlay to edit or cancel their own events; update/delete re-read the Graph ETag and fail safely on a conflict. Production create and edit are accepted with Zairo; final cancellation acceptance and disposable-event cleanup are pending explicit action-time confirmation. Mail/file writes remain off. | Scheduling can now stay in YAAZ; the only unfinished acceptance step is proving cancellation against the exact disposable test event and leaving the real calendar clean. | Create/edit/cancel and conflict-safe calendar behavior, or an explicitly accepted handoff boundary. | P1 | In progress |
| G04 | Microsoft Teams | Production remains on YAAZ `careon-m365` 0.8.0: joined teams and membership-checked channels load on demand with official HTTPS deep links. The earlier G04 source is preserved at `ccf9279`, and is now reconciled with G02/G05 in pushed branch `codex/m365-workplace-dormant` at `19f629e` as module 0.12. `M365_GRAPH_TEAMS_CONTENT_ENABLED` adds only delegated `ChannelMessage.Read.All` for paged inert-text root posts/replies after live revalidation; independent `M365_GRAPH_TEAMS_WRITE_ENABLED` adds only `ChannelMessage.Send`, depends on read and offers CSRF-protected bounded plain-text posts/replies with content-free audits and no automatic retry. The legacy global-write flag activates neither. The integrated suite is **380/380**; production flags remain `0` and no permission/consent changed. | Employees currently still hand conversations to Microsoft Teams. The internal reader/poster becomes usable only after explicit permission review, tenant consent, employee reconnect and live read/post/reply acceptance; chats, edit/delete, reactions, attachments and meetings remain separate. | Activate the least-privilege read and send surfaces independently after consent, then assess chat/meeting or richer-message increments separately instead of coupling broad permissions. | P1 | In progress |
| G05 | SharePoint / shared documents | Production remains on YAAZ `careon-m365` 0.8.0 with the accepted internal paged browser for canonical `All Company/Gedeelde documenten`; listing is metadata-only, subtree-bound and uses each employee's delegated ACL. The earlier G05 source is preserved at `b6ed6e3`, and is now reconciled with G02/G04 in pushed branch `codex/m365-workplace-dormant` at `19f629e` as module 0.12. When separately accepted, upload follows the currently open revalidated canonical folder and employees can create a subfolder there; OneDrive fallback is never writable. Upload is allow-listed to PDF/Office/text/CSV/JPEG/PNG up to 10 MB, uses server-derived MIME, fixed rename-on-conflict and send-once Graph calls. No overwrite/rename/move/delete/share/permission route exists. PHP lint and the integrated **380/380** rollback-only checks pass; production file writes remain `0` and no permission, consent, document or folder changed. | Employees can navigate approved folders today, but the canonical library is still empty and creation/upload stays in SharePoint until a content owner, structure/contributor policy and delegated `Files.ReadWrite.All` acceptance are supplied. Once those gates pass, ordinary folder/upload work can remain in YAAZ; editing and cleanup still use Microsoft. | Approve and populate/govern the canonical library, deploy the integrated dormant v0.12 line with writes off, then activate and accept only revalidated upload/subfolder creation; keep destructive and permission-changing file operations out unless independently justified. | P1 | In progress |
| G06 | EPD clinical work | Careon consumes five minimized, read-only exports. The current client export yields 1,059 parsed records and 1,055 dossier links; only the exact HTTPS TGC CareCheck origin and the two inventoried ZPM/uninsured route shapes survive import and the same policy is enforced again at render time. The boundary and vendor-contract checklist are documented. Notes, dossier content, appointments and clinical transactions remain read-only because no supported CareCheck API/SSO/write contract is held. | Employees can analyze signals in Careon and jump directly to an authorized CareCheck dossier, but they still create or change the legal clinical record in CareCheck. Careon does not pretend an unsupported portal automation is an audited clinical write. | Obtain the vendor's stable deep-link/SSO/API contract, then add narrowly scoped per-user read/write workflows with conflict, idempotency, audit and offboarding acceptance. | P0 | In progress |
| G07 | EPD data refresh | The five-source refresh still depends on a Windows interactive browser worker, but the exact `Careon TGC AI Import Worker` task publishes a metadata-only heartbeat every 30 seconds. Databron shows `available`, `offline` or `unknown`, while a secret-protected Vercel cron now checks the same heartbeat every five minutes. State transitions enter the existing audit log with only a coarse age bucket; `offline`/`unknown` keeps returning HTTP 503 so the outage remains visible in central observability. A missed heartbeat never drops the safely queued request. | The workstation/session dependency and weekly data-freshness limit remain, but worker unavailability is no longer silent when no administrator opens Careon. Maximum central detection is approximately 6.5 minutes. A named external notification destination and a vendor/centrally owned ingestion SLA are still missing. | Replace the interactive portal dependency with a vendor API or hardened centrally operated ingestion; until then, retain the heartbeat, safe queue, five-minute central failure signal and connect it to the TGC-owned escalation channel once its owner/destination is named. | P0 | In progress |
| G08 | Unified platform navigation | Careon opens YAAZ through the existing one-click OIDC handoff, and every authenticated YAAZ screen now has a persistent **Modules** entry back to the protected Careon launcher. Dashboard and Facturatie already expose the same module switcher. The external YAAZ link is HTTPS-only, fixed to `/modules`, disables cross-origin PJAX and disappears when misconfigured. | Employees can now switch Careon → YAAZ → Careon without a navigation dead end or a second product-specific password. The future native shell remains G09, not a prerequisite for a coherent web workplace. | A consistent shell, global navigation, identity/session behavior and return path across modules. | P1 | Done |
| G09 | Mobile workplace | Flutter shell `ebdad53` adds a repeatable cross-platform source/test/APK verifier and future commit-pinned read-only CI workflow on top of dormant meeting-contract head `402e8f5` (file bridge `d496ca6`, push `9e3ef5c`, Android build `beee34b`, handoff `516e2db`). Together with Careon `a914812`, the live handoff/push migrations and YAAZ `careon-sso` 1.3.1, it provides one-time WebView SSO, a fail-closed opt-in push receiver/device registry and governed native file handlers. The verifier pins Flutter/Dart, Microsoft JDK and Android packages; rejects tracked credential/signing material; enforces formatting, lockfile stability, zero analyzer findings and all **49/49** tests; and audits signing, app/SDK identity, exact permissions, backup/cleartext/Firebase/deep-link flags and absence of Jitsi/8x8 code. Two clean full runs produced identical APKs: debug `163,708,847` bytes / `5A0785A5...AFE44`, Android Debug/v2 signed; release `52,909,354` bytes / `0D5C9E2F...F2112`, intentionally unsigned. The 60-second module capability remains exact-user/org/entitlement/target bound and digest-only; FCM starts only after opt-in, accepted notifications remain metadata-only and file operations retain their allowlists. The local repository has no remote, so remote CI cannot run until TGC decides ownership/visibility. Production push delivery stays dormant because client Firebase/APNs credentials and a trusted sender do not exist; signed Android/iOS physical-device and store/MDM acceptance remain open. | Local reconstruction/build drift is now fail-closed and evidenced, while employees still receive the implemented one-login/file/push boundaries without platform/FCM token leakage or broad permissions. The app is not yet a supported pilot because real push, production signing, physical-device acceptance, distribution ownership and remote CI evidence are still absent. | Assign the canonical private repository, run its first CI, activate client-owned Firebase/APNs and the least-privilege sender, prove Android/iOS push/file/account-switch/offboarding behavior, then complete signing/privacy metadata and client-owned store/MDM rollout. | P1 | In progress |
| G10 | Calls and meetings | Sibling `humhub-meeting-modules` foundation `32d4484` plus packaging `7aeb52f` contain the isolated native Jitsi/JaaS proof and HumHub `meeting-core` 0.1.0. The exact reproducible archive is pinned and installed/enabled on production through `platform-deploy` `ca39055`/`0d2cf64`, after a verified DB + `/data` backup. Source/tree/version parity, **46/46** pure checks, **13/13** rollback-only runtime checks, recurring host health, guest redirect and Microsoft-primary SSO entry are accepted. Current shipping shell `ebdad53` retains the contract integrated at `402e8f5`: exact token parser, current-Supabase-subject binding, YAAZ-origin check, one-at-a-time confirmation boundary and sanitized lifecycle channel behind independent build-request and SDK-adapter gates. Its repeatable gate passes **49/49** tests and proves clean debug/release API 24/36 builds with no Jitsi/8x8 artifact or media permission. Both server kill switches remain `0`; the shell build flag is false and no SDK adapter, meeting navigation, real JaaS credential/key/token or shipping-shell media permission exists. | The backend authorization/signing, reproducible deployment and production-shell contract boundary now exist, but employees still use external meeting tools because no TGC-owned JaaS tenant/key or native SDK adapter has been activated and no signed physical-device call has been accepted. | Provision and govern JaaS, validate tenant ownership/DPA/region, add the pinned production native SDK adapter and narrowly reviewed media declarations, then prove signed Android/iOS joins plus denial/retry, interruption, audio/Bluetooth, permission, account-switch and offboarding behavior before a separately reviewed activation. Recording/transcription/AI remain a separately consented Phase 3. | P2 | In progress |
| G11 | Organization structure | Production remains unchanged on `careon-spaces` 1.0.0 with exactly one company-wide `TGC Groep` Space (5 active members, 1 Space admin, no pending memberships/content). Pushed isolated branch `codex/g11-space-apply` at `3adcc4c` prepares 2.0.0: a production-off transactional apply capability requiring the exact reviewed plan hash, a fresh verified backup stamp and fixed confirmation. The hash now commits privately to exact identity resolution and current managed-Space ACLs; apply uses an organization lock, serializable transaction, primary-owner transfer, exact membership/leave policy, four allow-listed optional modules, durable private Team/policy bindings, identity-free audits and a post-write re-plan. There is no Space-delete path. The pinned local runtime passes **42/42** policy/redaction checks plus **42/42** rolled-back create/reconcile/isolation/failure checks. The branch is not merged/deployed and `CAREON_SPACE_APPLY_ENABLED` remains `0`. Zairo's seven joined Teams remain candidates, not organization-wide membership authority. | Collaboration still lacks real departmental/private work areas until TGC IT approves each Team mapping, complete member/owner list and retention identifier. The execution engine is release-ready without weakening that governance boundary; production ACLs are unchanged. | Obtain the authoritative TGC IT manifest, merge/deploy 2.0.0 with apply still off, review the resulting live plan, create a fresh backup, then run one controlled apply/non-member acceptance and immediately return the capability to `0`. | P1 | In progress |
| G12 | Facturatie readiness | Production now has a clean empty TGC administration: the five unissued development concepts were removed through a count/signature/semantic-guarded transaction and a metadata-only audit event; no contacts, legal sender settings, invoices, PDFs or mail attempts remain. The private bucket and database boundary are healthy, dedicated foreign-key indexes are live, and invoice finalization/crediting no longer exposes an authenticated `SECURITY DEFINER` RPC: only the service role can call the replacement, after the application and database both revalidate the initiating administrator. Mail remains deliberately fail-closed. | The module is technically clean and protected, but real invoicing must not start until TGC supplies and validates its legal sender/contact data and the Resend/DPA/domain gates are complete. | Enter and validate legal data, complete mail/DPA activation and run one controlled invoice/PDF/download/mail acceptance cycle. | P0 | In progress |
| G13 | Production content/data | Production YAAZ contains only the five real platform accounts. The five fictional users, July showcase feed, local demo agenda and three fixture conversations were removed through an exact-signature cleanup; `careon-demo` is disabled. The real TGC Space, five memberships, five Careon OIDC bindings and Zairo's Microsoft connection were preserved. | Demo material can no longer be mistaken for company information. Approved real department structure and shared content remain separate G05/G11 rollout work. | Separate demo and production content, import approved real structure/content and label or remove fixtures. | P0 | Done |
| G14 | Brand and language | The Dutch wordmark/copy/UI boundary is live. Canonical target `https://yaaz.careonpulse.com` is now fully prepared in local-only platform release `ec3eb36`: exact legacy 308/no-cookie redirect, host/callback drift gates, rollback runbook and deterministic external smoke checks. The existing confidential Supabase OAuth client now accepts localhost, the old sslip.io callback and the new canonical callback exactly. Fresh production DB/data pair `20260822-054010` plus a root-only checksum-verified code/environment archive form the rollback point. Entra, DNS, host environment/base URL and Careon/Vercel launcher remain unchanged until the exact Entra callback addition is confirmed, so production still uses sslip.io. Supported HumHub attribution also remains. | The cutover is fail-safe and recoverable but not yet user-visible; the temporary hostname and supported vendor attribution still prevent a complete company-owned presentation. | Complete the backed-up canonical-domain cutover and acceptance, review remaining Dutch copy with the client, and use a licensed/supported HumHub white-label route if attribution must be removed. | P1 | In progress |
| G15 | Offboarding/session revocation | The hourly reconciler is live and production-accepted. A controlled disposable employee disablement produced one bounded offboarding action, made the Careon session return `authed:false`, invalidated the existing YAAZ session, marked only that YAAZ account disabled and left every real employee untouched. | The former unproven cross-plane offboarding risk is closed; ongoing operations must monitor cron failures and the documented secret expiry. | Automated eligibility reconciliation, immediate Careon block/session revocation, Graph disconnect and evidenced SLA. | P0 | Done |
| G16 | Reproducible source and deployment | Stable reviewed controls remain committed. Latest code-bearing Careon head `1c7a421` repairs the clean-clone quality gate: all five EPD parsers always receive committed synthetic coverage while the private gitignored exports receive an additional local-only pass. GitHub run `32561997452` has green quality and **130** browser scenarios; analysis run `32561997480` is green; Vercel `dpl_2PGWaAi6geCqndQxGigs8hJ5zUTN` is Ready on every production alias and bound to that exact commit. Six owner edits remain deliberately unstaged. YAAZ production remains on module 0.8.0. Canonical-domain release `ec3eb36` is intentionally local-only pending its exact Entra callback confirmation. The three preserved source lines (`c0a0e80`, `ccf9279`, `b6ed6e3`) are reconciled as feature tree `19f629e` in clean pushed branch `codex/m365-workplace-dormant`; head `059172b` adds a no-secret disposable HumHub/Calendar/M365 CI gate, independently green in run `32560319936`. Module 0.12 passes PHP lint, fresh install/migrations, **380/380** rollback-only checks and source/runtime parity while every new production capability stays `0`. Governed Space apply remains independently isolated at `3adcc4c`. Shell head `ebdad53` is clean and locally reproducible with a commit-pinned future CI workflow; it remains local because the repository has no remote and TGC ownership/visibility was not invented. Meeting source/package remains at `7aeb52f`. No pending permission/capability changed in production. | The overwrite/reconstruction risk is closed across Careon, M365 and the local mobile build: a clean clone no longer depends on private EPD files, one coherent M365 candidate replaces three diverging lines, and the shell has a single fail-closed source/test/binary policy. Remote shell CI evidence still waits on a governance decision, not missing engineering code. | Preserve commit-first isolated-release discipline; assign and publish the shell repository only after ownership/visibility is explicit, then run its first CI. Deploy the integrated M365 candidate dormant only in a backed-up reviewed window, and activate each capability solely after its own config, consent, reconnect and acceptance gates. | P0 | Done |
| G17 | Backups, monitoring and operations | Local restore rehearsal, active nightly backup/five-minute full-plane health and hourly external smoke remain green. Platform `5a9d63c`/host `7498990` makes YAAZ R2 publication pair-aware and recovery exact-pair/no-overwrite. Careon `8df360b` keeps the Facturatie secondary-copy path client-side AES-256-GCM encrypted, EU-only, conditional/no-overwrite and completion-last, and now adds a daily Windows schedule boundary plus central metadata-only status. The service-only status/RPC records only healthy/failed, a fixed failure code and database timestamps; the five-minute monitor derives healthy/failed/stale/unknown and reuses the durable operations outbox. Focused checks are 120/120 operations plus 64/64 Storage; full CI is 1,927 deterministic/556 hygiene, zero vulnerabilities, build and 130/130 browser scenarios green. Live rollback acceptance left no residue; RLS/grants are fail-closed. The task is not installed and all client flags remain off: no TGC-owned R2 destination/token, approved external key custody or always-on operator has been supplied, so no R2 write or invoice-byte copy occurred. | The software can now schedule, monitor and externally escalate an encrypted Facturatie backup without leaking invoice or storage metadata, but server loss can still lose local-only YAAZ backups and future invoice/logo objects until client destinations are activated. Neither plane yet has an owned Teams destination, and the off-site RPO remains open until a real non-empty restore is proven. | Supply and approve the TGC-owned EU R2 destinations, least-privilege tokens, lifecycle rules and external key custody; activate the daily task on a managed always-on host; prove isolated YAAZ fetch+restore plus non-empty Facturatie fetch/restore; then name a Teams channel with service-principal or redundant owner and run controlled incident/recovery delivery acceptance. | P0 | In progress |

## G01 — Entra-driven employee onboarding

### Recommendation

Use a **split source-of-truth model**:

- **Microsoft Entra ID is authoritative for employee identity and platform eligibility.** With TGC's current licensing, TGC-IT assigns approved employees directly to app role `Careon.User`; after P1/P2 is licensed for the eligible workforce, a dedicated `Careon Pulse — Users` security group can replace the direct list.
- **Careon remains authoritative for organization membership details, Careon roles and module entitlements.** Directory membership never grants `org_admin`, superadmin, financial or clinical privileges automatically.
- **Normal employees do not need a Careon invitation or password.** Their first successful Microsoft login creates or links the Supabase identity and creates a default TGC `member` membership, but only if all eligibility checks pass.
- **A personal Microsoft 365/Graph data connection remains per-user.** Tenant-wide admin consent can remove the consent prompt, but delegated Outlook/Teams/SharePoint access still requires the employee to authenticate once; it must not be fabricated or impersonated in the background.

This is safer than treating all roughly 65 tenant directory objects as employees. A tenant can also contain guests, shared mailboxes, service identities, contractors and dormant accounts.

**Tenant prerequisite:** Microsoft documents group-based enterprise-application assignment as requiring Entra ID P1 or P2. Verify TGC's licensing before G01-A. If it is unavailable, retain the same safe JIT design but use direct app assignments as the interim eligibility list (or add the required licence); do not fall back to authorizing the entire tenant. See [Microsoft — manage users and groups assignment to an application](https://learn.microsoft.com/en-us/entra/identity/enterprise-apps/assign-user-or-group-access-portal).

### Confirmed sign-in/provisioning flow (D22)

1. TGC-IT assigns an approved employee to app role `Careon.User` (directly under the current licence, later through `Careon Pulse — Users`); the enterprise application has **user assignment required** enabled.
2. The employee chooses **Inloggen met Microsoft**. Entra enforces the tenant's MFA and Conditional Access.
3. Supabase creates a new auth identity or automatically links the Azure identity to an existing user only when the verified e-mail matches.
4. The Careon callback validates, server-side, the Azure provider, exact configured tenant ID (`tid`), tenant-member claim (`acct=0`), verified-email claim (`xms_edov`) and normalized e-mail.
5. In one audited transaction, Careon creates the missing TGC membership as role `member`; it never promotes automatically.
6. The employee lands in the module launcher. Existing module entitlement rules determine visible modules.
7. On first YAAZ launch, the existing Careon OIDC flow provisions the regular YAAZ identity. The employee connects Microsoft 365 once when personal Graph data is needed.

### Directie employee-management experience

Careon Directie now includes an admin-only **Microsoft medewerkers** view alongside the current membership manager. It reads the complete Entra identity inventory for management visibility, while treating the configured `Careon.User` app-role assignment as a separate authorization fact. Seeing an identity in the inventory never grants access. Guests, disabled accounts and unassigned users remain visible but fail closed for Careon login. The view reconciles:

| Field | Meaning |
|---|---|
| Entra status | Member/guest, active/disabled and licensed/unlicensed |
| Careon eligibility | Assigned/unassigned to the exact `Careon.User` app role |
| Careon status | Not started, active, blocked, role and module entitlements |
| YAAZ status | OIDC account provisioned or not yet opened |
| Microsoft 365 status | Personal Graph connection present, absent, expired or revoked |
| Last activity | Last Careon login and last successful reconciliation |
| Required action | Ready, first login pending, role review, disconnect or offboard |

For a read-only interactive directory view, Microsoft Graph's user/group APIs can be called with delegated administrator permissions. For a reliable background source of truth and offboarding, use a **separate provisioning connector/app (or SCIM if the chosen identity components support it)** rather than reusing either the identity-only D20 registration or the personal-data D21 registration. This preserves their intentionally separate failure domains and permissions.

### Delivery phases

| Phase | Scope | Result |
|---|---|---|
| G01-A | Curated app-role assignment source, tenant/verified-email validation, assignment-gated just-in-time `member` creation, audit event and tests. | New eligible employees need no Careon invite; first Microsoft login provisions safe default access. |
| G01-B | Admin-only full Entra inventory with Careon eligibility, Careon/YAAZ reconciliation and status filters; explicit assignment/entitlement writes remain a later least-privilege operator action. | Management can see every Office identity and distinguish directory presence from authorization in Careon Directie. |
| G01-C | Scheduled/delta or SCIM-style reconciliation, disable/remove handling, Supabase session revocation and YAAZ Graph disconnect. | Entra eligibility changes reliably propagate to the full platform. |

### Security invariants

- Never authorize by e-mail domain alone.
- Require the configured tenant ID, `acct=0`, verified e-mail and approved app-role eligibility.
- Never infer Careon administrative roles from broad Entra directory roles.
- Do not expose Graph directory tokens to the browser or reuse the D21 personal-data token store.
- Keep current membership/session checks fail-closed during rollout.
- Log provisioning, eligibility mismatch, role change, Careon blocking and Graph disconnect events.
- Retain break-glass/platform-admin access outside the customer tenant as established in D20.

### Blueprint decision

**Approved by the owner on 21 August 2026 and recorded as confirmed D22 in blueprint v2.4.** D22 amends D20's former administrator-only/no-JIT provisioning rule while leaving the hybrid login architecture and break-glass paths intact.

Accepted consequences of the proposed change would be:

- **Benefit:** no manual invitation/password lifecycle for ordinary TGC employees and a scalable path from about 5 memberships to the eligible workforce.
- **Security change:** Entra group administration becomes a production authorization dependency, so group owners, change auditing and emergency removal must be explicit.
- **Permissions/scope:** the eventual admin inventory and automated reconciliation require additional directory/group read capability in a separate least-privilege connector.
- **Licensing:** group-based enterprise-app assignment requires Entra ID P1 or P2; direct user assignment is the safe fallback if TGC does not have it.
- **Engineering:** atomic JIT provisioning, idempotency, concurrency tests, audit logs, reconciliation and offboarding work are required; a simple bulk import is insufficient.

### Acceptance criteria

- An eligible new TGC employee signs in with Microsoft and receives exactly one Supabase identity, one TGC `member` membership and one YAAZ identity without a Careon invitation.
- An unassigned same-tenant user, guest, wrong-tenant user and unverified/mismatched e-mail all fail closed and receive no membership.
- Existing password accounts link only on exact verified-e-mail equality; duplicates are not created.
- Concurrent first logins remain idempotent and cannot create duplicate membership or audit records.
- No provisioning path can create `org_admin`, superadmin or confidential module access automatically.
- Removing/disablement meets the agreed revocation SLA across Careon, Supabase sessions and YAAZ Graph tokens.
- The Directie view reconciles observed Entra eligibility with Careon/YAAZ state and exposes no directory data to ordinary members.
- Production evidence covers success, denial, linking, duplicate prevention, role isolation, offboarding and audit visibility.

## G06 — CareCheck clinical-work boundary

### Implemented boundary

- The five production exports remain minimized and read-only; Careon is the
  analytics/signals surface, not the legal dossier.
- `Ga naar dossier` is untrusted input. Import accepts only HTTPS on exact
  origin `tgc.zsg.nl`, without credentials/query/fragment, and only the two
  inventoried `/dossier/zpm/.../...` and `/dossier/uninsured/...` shapes.
- The patient attention panel and KPI drill tables reapply the same policy
  before rendering a separate-tab link.
- Current-export verification is aggregate-only: it proves every retained URL
  satisfies the boundary without logging an identifier or patient row.
- The complete authority model, forbidden portal-automation path, staged
  vendor integration and acceptance matrix live in
  `docs/CARECHECK_INTEGRATION_BOUNDARY.md`.

### Remaining boundary

Careon must not add clinical notes, plans, signatures or appointment writes
until TGC IT/CareCheck supplies a documented SSO/API contract covering per-user
authorization, audit attribution, stable identifiers, idempotency,
ETag/version conflict behavior, webhooks/deltas, sandbox, region/DPA and
offboarding. Headless portal form automation is explicitly not an accepted
substitute.

### Acceptance criteria for the next stage

- An employee can open only a dossier they are authorized to see in CareCheck;
  account switch and offboarding fail closed.
- No Careon/Supabase token, portal cookie, clinical free text or patient
  identifier enters a URL/log beyond the vendor's own opaque dossier path.
- Any future write is attributed by CareCheck to the initiating employee and
  returns an immutable record/correlation ID.
- Replays are idempotent, stale versions conflict safely, vendor failure is not
  presented as success, and CareCheck remains independently reachable.
- The controller/vendor approve the data-processing and legal-dossier boundary
  before clinical write activation.

## Cross-session update template

When work starts or finishes, update the relevant row and append concise evidence here:

```text
Date:
Gap / phase:
Status change:
Code/config deployed:
Verification evidence:
Remaining external action / owner:
Decision reference:
```

## Evidence log

- **22 Aug 2026 — independent M365 installation CI green:** GitHub Actions run `32560319936` completed successfully for exact platform branch head `059172b`. Every executed step is green: checkout, fail-closed no-secret test configuration, pinned stack startup, fresh HumHub provisioning, exact Calendar 1.8.16 installation, M365 verification/parity and always-run teardown. The predecessor run failed on the previously implicit Calendar marketplace dependency; that failure was not bypassed. The final workflow asserts the production-pinned Calendar version before enabling it, so a future marketplace drift or missing dependency fails closed.
- **22 Aug 2026 — G02/G04/G05 reconciled into one dormant Microsoft 365 0.12 candidate with CI, production unchanged:** pushed branch `codex/m365-workplace-dormant` feature tree `19f629e` applies the preserved Teams reader/poster, Outlook reply/attachment and SharePoint current-folder write commits in their reserved version order; head `059172b` adds a path-filtered, least-privilege GitHub Actions gate. Conflict resolution retained every independent flag and dependency: attachment access requires mail content but adds no scope; Teams send requires Teams content and is never activated by the legacy global-write flag; SharePoint writes remain behind the file-write gate and can target only a re-read canonical drive/subtree folder. `GraphClient` carries read retry, bounded binary download and explicit send-once POST/PUT behavior together. All module PHP files lint clean and the integrated transactional verifier passes **380/380** with database rollback. The CI procedure was independently rehearsed locally: a disposable pinned HumHub/MariaDB/Redis stack applied 205 core migrations, installed and asserted Calendar 1.8.16, applied 23 Calendar plus one M365 migration, repeated **380/380**, proved module hash `b74698d7...61d3` and removed its containers, volumes and non-secret local config. The shared runtime was restored to exact 0.8. The branch is pushed but not merged/deployed. Production flags remain `0`; no Entra permission, admin consent, employee token, message, file or folder changed.
- **22 Aug 2026 — G14 canonical YAAZ domain prepared through the additive OAuth boundary; cutover pending:** platform commit `ec3eb36` remains local-only and adds exact `yaaz.careonpulse.com` production policy, a read-only imported Caddy compatibility endpoint, canonical login/legacy path+query/no-cookie verification, self-tests and an external monitor that executes the same contract. Vercel DNS ownership and the unused wildcard-backed hostname, current Hetzner stack, HumHub base URL, Careon launcher URL, confidential Supabase client and the separate Entra Graph registration were inventoried without secret exposure. The Supabase client update was applied transactionally with exact old/expected guards and now retains both old callbacks while appending the canonical Careon→YAAZ callback. Production backup pair `20260822-054010` verifies at age four seconds after creation; the separate deployment code/environment archive verifies by SHA-256 and both files are mode 600. Entra still has only the old Web callback, and DNS, host environment/base URL and Vercel launcher were not changed. The next live step is the exact additive Entra callback confirmation; old callbacks/redirect remain during acceptance and their later removal is a separate reviewed action.
- **22 Aug 2026 — G09 reproducible mobile source/APK gate committed locally; gap in progress:** shell `ebdad53` adds `scripts/verify-shell.ps1`, a documented artifact/signing policy and a future GitHub workflow with read-only permissions and checkout/Java/Flutter actions pinned to immutable commits. The verifier resolves and checks Flutter 3.47.1/Dart 3.13.1, Microsoft OpenJDK 17.0.20.x, command-line tools 22.0, platform tools 37.0.1, API 34/36, Build Tools 36.0.0, NDK 28.2.13676358 and CMake 3.22.1. It rejects tracked credential/private-key/signing files, release signing config, formatting or lockfile drift; runs zero-warning analysis plus **49/49** tests; builds from the fail-closed production example; then inspects app id, API 24/36, the exact six-permission set, debug v2 certificate, deliberately unsigned release, backup/cleartext/Firebase/deep-link flags and absence of Jitsi/8x8 packages. Two consecutive full runs produced identical debug and release sizes/hashes (`163,708,847` / `5A0785A5...AFE44`; `52,909,354` / `0D5C9E2F...F2112`). No credential, production capability or external system changed. The shell worktree is clean, but no Git remote exists; TGC must decide the canonical owner and public/private visibility before publication and the first remote CI run. Signed physical Android/iOS and store/MDM acceptance remain under G09.
- **22 Aug 2026 — G16 clean-clone TGC parser gate repaired and accepted:** the documentation release `8f28481` surfaced an honest CI failure: `verify:tgc-sync` required the ignored private `Exports EPD` folder even though clean GitHub runners must never receive those files. Careon `1c7a421` separates the boundaries. It always checks configuration plus all five production parsers with synthetic committed inputs and a valid exact TGC CareCheck link; if private exports exist locally, it additionally validates the newest complete five-file set through the same parsers. Both present/absent-directory local paths pass, the entire credential-free CI chain and 107-route build are green, and no private row/file was committed or uploaded. GitHub `quality` plus all **130** browser scenarios are green in run `32561997452`; analysis run `32561997480` is green. Vercel `dpl_2PGWaAi6geCqndQxGigs8hJ5zUTN` is Ready on every alias with the commit-bound status success; live/readiness/Microsoft entry and initial error/HTTP-500 scans pass. This preserves G16's clean-clone claim instead of weakening the parser verification.
- **22 Aug 2026 — G04 dormant text-only Teams channel read/post/reply boundary committed and pushed; gap in progress:** branch `codex/g04-channel-messages` head `ccf9279` prepares `careon-m365` 0.10.0 without changing production. The read gate is exactly delegated `ChannelMessage.Read.All`; the second write gate adds only `ChannelMessage.Send`, depends on read and cannot be activated by the legacy global-write flag. Every read or write revalidates the current employee's joined team and selected channel. Reads page twenty root posts/replies and normalize bounded inert text; HTML/tracker images, hosted-content bytes, reactions and attachment names/URLs/bytes never reach the view or storage. Writes are authenticated CSRF-protected POST only, accept at most 8000 normalized text characters, send only Graph `body.contentType=text` plus content and log only a fixed action kind. Path/control/oversize/empty input fails before Graph. There is no chat, attachment, reaction, edit/delete or automatic retry/idempotency claim; a successful POST with a missing response ID is not repeated. PHP lint, Compose/config, source/runtime parity and Dutch UI checks pass; rollback verification is **310/310**. Both production flags remain `0`; the branch is not merged/deployed and no Entra permission/consent changed. Current production stays 0.8.0 pending separate consent, reconnect and live read/post/reply acceptance.
- **22 Aug 2026 — G07 central TGC worker monitor live; gap in progress:** Careon `dc931e0` adds a secret-protected five-minute Vercel cron over the existing service-only heartbeat. It reads only the TGC organization ID, latest heartbeat timestamp and previous monitor action; exact timestamps are reduced to a coarse age bucket, and no queue payload, production snapshot, patient row, Windows identifier or portal credential enters the monitor or its audit detail. State transitions are synchronously written as `tgc_worker.available`, `.offline` or `.unknown`; an unavailable state keeps returning HTTP `503` on every run so central observability cannot become quiet after the first transition. Focused verification is **63/63**; full CI passes **1,839** checks (**1000** product, **423** production, **145** assistant, **105** runtime, **70** mobile, **33** Storage and **63** queue) with zero high-severity dependency findings. The 107-route build and all **130** Playwright desktop/mobile/WCAG scenarios pass. Vercel `dpl_6fH33EYfPfzrQycypU1T5Jeks5AZ` built exact `dc931e0`, is Ready on every production alias and registered the new cron. Anonymous production access is `401` with `no-store`/`nosniff`; the first automatic authorized invocation returned `200`, and Supabase contains exactly the corresponding metadata-only transition `available`, previous `null`, age `under_2m`. No import was triggered. G07 remains `In progress` because the interactive portal/workstation and weekly data cadence still need a vendor API or central ingestion host, and TGC must name the external notification owner/destination before e-mail, Teams or pager delivery can be connected.
- **22 Aug 2026 — G07 durable Teams Workflows delivery boundary deployed dormant; gap in progress:** Careon `7f179e4` plus migration `20260823000000_operations_alert_outbox` (live record `20260822043525`) atomically serializes and deduplicates each organization worker transition, audit event and service-only forced-RLS outbox row. Five security-invoker RPCs are executable only by `service_role`; table privileges are select/insert/update with no delete, while `anon` and `authenticated` have no table or RPC access. Atomic `SKIP LOCKED` claim, ten-minute stale-lease recovery and 1m/5m/15m/1h/6h retry keep external HTTP outside database transactions; the fixed metadata-only Teams payload uses a stable opaque incident UUID for deliberate at-least-once delivery. The callback remains a server-side secret and must match its exact current Power Automate host/SHA-256; deprecated Connector/`logic.azure.com` paths, redirects, credentials, arbitrary text and response-body reads are rejected. Full CI passes **1,904** deterministic checks plus **551** hygiene checks, focused queue coverage is **97/97**, dependencies are clear, the 107-route build passes and every one of 130 browser scenarios passed (one Chromium-start retry, no assertion failure). A rollback-only incident→retry→recovery acceptance left zero residue. Vercel `dpl_3HrUbnJ7dBWzTKPuRFuVc6jeQjzi` built exact `7f179e4`, is Ready on every production alias, and its first automatic cron returned `200`; live/readiness/Microsoft/anonymous denial and initial error/HTTP-500 scans are green. Production delivery is intentionally disabled, current worker remains `available` and the outbox has `0 due / 0 outstanding`. G07 still needs a supported vendor API or central ingestion host, a data-freshness SLA, and a TGC-named Teams channel with service-principal or redundant TGC IT ownership plus controlled offline/recovery delivery acceptance.
- **22 Aug 2026 — G06 safe CareCheck dossier handoff deployed; gap in progress:** Careon `8e82d26` replaces the former generic-HTTPS rule with one shared normalizer/type guard for exact `https://tgc.zsg.nl`, no credentials/query/fragment and only the two observed ZPM/uninsured dossier route shapes. The parser discards every other value before snapshot storage; the patient attention panel, drill-column selection and link cell independently fail closed at render time. A privacy-minimized current-export pass parsed **1,059** records, retained **1,055** valid dossier links, stored zero invalid links and emitted only two existing parse warnings; no patient row or identifier was printed. The permanent synthetic matrix covers valid ZPM/uninsured, JavaScript/HTTP, wrong host/host suffix, query, fragment, userinfo and unknown-path cases; the mixed fixture proves valid links render while foreign origins disappear. The new vendor boundary forbids browser-driven clinical writes and records the SSO/API/audit/idempotency/concurrency/DPA contract required for deeper integration. Full CI is green (**1000** product, **423** production, **145** assistant, **105** runtime, **70** mobile, **33** Storage and **46** queue checks; current-export sync verification and zero-vulnerability audit pass). The 106-route build succeeds. All **130** logical Playwright desktop/mobile/WCAG scenarios passed; one Chromium context-start flake passed its automatic retry and the focused repeat passed **1/1**. Vercel `dpl_2zPZAYcCz6HsiRHJpXg8SqLgHh3D` built exact `8e82d26`, is Ready on every production alias, reports live/readiness `200`, retains the Microsoft `303` entry and has zero initial error/HTTP-500 log entries. G06 remains `In progress`: TGC IT/CareCheck must provide the supported vendor contract before any clinical read/write expansion.
- **22 Aug 2026 — G17 off-site backup/recovery boundary deployed fail-closed; gap in progress:** platform `5a9d63c` adds all-or-none EU-only R2 configuration, `BACKUP_OFFSITE_REQUIRED`, per-object SHA-256/pair/kind metadata, a manifest written only after both objects, immediate and recurring remote verification, encrypted-pair handling, and a no-overwrite exact-stamp recovery fetch that validates manifest, hashes, sizes, metadata and mode 600 before publishing locally. The destructive `restore.sh` remains a separate exact-confirmation action. Its credential-free failure matrix is **18/18**, covering disabled/required/partial/non-EU configuration, stale or unsafe local pairs, manifest/hash drift, corrupt downloads, overwrite/root-output refusal and encrypted-pair headers. A fresh production pair `db-20260822-005429.sql.gz` + `humhub-data-20260822-005429.tar.gz` and pre-change code archive `code-20260822T005453Z-pre-5a9d63c.tar.gz` preceded rollout. Host release `7498990` includes the operations runtime plus LF release policy. Production reports `BACKUP=OK … encrypted=0`, `OFFSITE=DISABLED required=0` and `HEALTH=OK workers=4 … offsite=disabled`; both timers are enabled/active and the health/backup service results are success. No R2 credential, bucket or external write was invented. G17 remains `In progress` for TGC-owned private EU R2 activation/lifecycle rules, one isolated live fetch+restore, secondary Facturatie Storage backup, owned external alerts and scheduled secret rotation.
- **22 Aug 2026 — G07 worker availability is live; gap in progress:** Careon `925dd04` adds worker version 1.1.0, a startup/30-second metadata-only heartbeat and the Databron `available`/`offline`/`unknown` presentation; `cd7b350` makes every TGC sync success, error and denial response `private, no-store`, `Vary: Cookie` and `nosniff`. Live migration `20260822233000_tgc_worker_heartbeat` creates the organization-scoped forced-RLS heartbeat table and an empty-search-path `SECURITY DEFINER` RPC executable only by `service_role`; authenticated RPC use and direct authenticated/service-role table mutation were explicitly denied, while a rollback-only service call passed. The formerly idle exact Windows task `Careon TGC AI Import Worker` was restarted and a recurring live heartbeat—not only startup—was observed. Focused queue verification is **46/46**; full Careon CI passes (**1000** product, **412** production, **145** assistant, **103** runtime, **70** mobile and zero npm vulnerabilities), and the 106-route build succeeds. Vercel `dpl_7Hhxna5N6nDhNCSdn5n5SKPVzecK` is exact `cd7b350`/Ready. Anonymous production probing returns a private `401`; authenticated Zairo acceptance shows **TGC-worker beschikbaar**, the API reports `available`, administrator navigation includes Databron/Gebruikersbeheer and browser warnings/errors are zero. No new EPD import was started. G07 remains `In progress` because the interactive workstation/portal session, weekly schedule and unowned external alert still prevent a central ingestion SLA.
- **22 Aug 2026 — G09 native shell foundation and production registry live:** sibling repo `careonpulse-shell` is initialized at local commit `370a28a` with Flutter 3.47.1/Dart 3.13.1, approved Careon artwork, separate OAuth callback/deep-link schemes, public-client authorization-code + PKCE, Android Keystore/iOS Keychain token storage, proactive refresh + one 401 retry, Dutch launcher states, strict schema/minimum-version checks, per-tile enablement, entitlement-aware deep links and HTTPS-host-restricted WebViews. `flutter analyze` and **12/12** tests pass; Android backup/cleartext/debug-release-signing are disabled and phase 1 declares no media/location permissions. Supabase client `cb42abd3-aed8-49d4-9126-0fa42da9d597` is verified public with `none`, no secret, exact `com.careonpulse.app://oauth/callback`, authorization-code + refresh-token grants. Careon commit `4873a67` adds the exact-client-bound, RLS-backed, no-service-role registry and **25/25** contract checks; full CI passed (1000 product, 412 production, 145 assistant, 103 runtime, 34 queue; zero high npm vulnerabilities) and the Next.js build succeeded. Vercel `dpl_AqdT4owwzdKoAix3uGioCQd9Xex5` is exact `4873a67`, Ready/Production on all aliases. Live anonymous and malformed-bearer probes return `401` with `private, no-store`, `Vary: Authorization` and `nosniff`; readiness is `200`. G09 remains `In progress` for one-time module session handoff, push, file bridges, signed device acceptance and client-owned distribution.
- **22 Aug 2026 — G09 one-time module session handoff live:** Careon `a2f9b7f` adds the two mobile handoff endpoints and service-only hash ledger; Supabase migrations `20260822210000`/`20260822213000` are applied with forced RLS, no anon/authenticated table or RPC access, service-only execution, digest-only storage and atomic one-time consume. The 60-second code is bound to the exact current user, TGC membership, entitled module and origin-safe target; consumption rechecks ban/membership/role/entitlement and establishes only an HttpOnly/Secure first-party session. Flutter `516e2db` POSTs that code from memory after refresh/retry and never persists it. YAAZ `a60de23`/`careon-sso` **1.3.0** preserves only a bounded origin-relative deep link through the normal OIDC flow; malformed absolute/protocol-relative/control destinations fail closed. Supabase Auth and Vercel now share canonical `https://www.careonpulse.com`. Careon full CI (**1000/412/145/103/43/34**, no high npm findings), its 106-route build, Flutter analyze + **16/16**, YAAZ SSO **34/34**, fresh backup, source/runtime hash parity and host health are green. Vercel `dpl_5xfXJ3b5CJiEZjVCeqf1scynVJNK` is exact `a2f9b7f` and Ready. Live Zairo acceptance followed the complete Careon → YAAZ → Supabase consent → YAAZ callback path to `/microsoft-365` with `200`, no second login and `401` on replay; the same harness with a foreign demo-organization identity was refused. No active raw capability is stored. G09 remains `In progress` only for push, native file bridges, signed physical-device acceptance and client-owned distribution.
- **22 Aug 2026 — G09 reproducible Android build gate established:** shell commit `beee34b` pins Flutter 3.47.1/Dart 3.13.1, Microsoft OpenJDK 17.0.20.1, Android API/target 36, Build Tools 36, NDK 28.2 and CMake 3.22.1 in a checksum-backed runbook. The first build exposed an upstream `flutter_secure_storage` 11.0.0 incompatibility with Google's minor-versioned `platforms/android-37.0`; the shell now pins the last compatible secure-storage 10.3.1 release and minimum API 24 until the plugin adopts the minor-aware Gradle DSL. Analyze and **16/16** tests remain green. A 161,258,392-byte debug APK verifies with APK Signature Scheme v2 and only the expected `Android Debug` certificate; application id `com.careonpulse.app`, min/target 24/36, backup off, cleartext off and Internet-only functional permission were independently read from the packaged manifest. Release mode also compiles to a 51,276,797-byte APK and correctly fails signature verification because client-owned release signing is deliberately absent. This closes local Android build-environment uncertainty without claiming distribution readiness; physical-device install/flow acceptance, the TGC upload key/Play account, privacy declarations and managed rollout remain open.
- **22 Aug 2026 — G09 opt-in push receiver and secure device registry deployed dormant:** shell `9e3ef5c` adds Firebase Core/Messaging with Android/iOS auto-init off, explicit in-app consent, secure installation identity, token registration/refresh/revocation, inert foreground display and strict title + `careonpulse://` payload parsing. Every tap still uses the server registry, current entitlement and a fresh one-time handoff; body text, extra fields, external/credentialed/fragment links and control characters fail closed. Flutter analyze and **27/27** tests pass. Debug and release APKs compile; the 51,990,000-byte unsigned release artifact hashes to `DF9592A3B96E8021EA94C23AF99554CE94ECFD95FA939F567EA63B80D1E02368`, keeps backup/cleartext and Firebase Messaging/Analytics auto-init off, and adds only expected FCM/notification/network permissions. Careon `3414dfb` adds the exact-shell-client bearer API, 8 KiB request limit, no-store audit-safe responses and AES-256-GCM/HMAC token protection. Live migration `20260822230000` creates the service-only forced-RLS registry and atomic cross-account reassignment/offboarding RPCs; anon/authenticated table and function access is denied. A fresh sensitive 256-bit server key is present in Vercel Production/Preview without value exposure. Full Careon CI passes (**1000/412/145/103/62/34**, zero npm vulnerabilities), the 106-route build is green and Vercel `dpl_Fr7oHVCDaha5mEr5wVofhGo6MSnH` is exact `3414dfb`/Ready. Live anonymous and malformed-bearer PUT/DELETE probes return `401` with `private, no-store`, `Vary: Authorization` and `nosniff`; readiness is fully green and the device table remains at zero rows. This is receiver/registration-ready, not active delivery: TGC must still own/configure Firebase and APNs, the trusted sender must be implemented, and real Android/iOS delivery/account-switch/offboarding acceptance must pass.
- **22 Aug 2026 — G09 governed native file bridge build-ready and deployed:** shell `d496ca6` adds a strict file policy plus Android OS document picker, authenticated same-origin invoice fetch and Android/iOS OS save/share sheet. Uploads are intersected with a fixed CSV/TXT/PDF/PNG/JPEG/DOC/XLS allowlist, at most five files, 16 MiB each and 32 MiB total. Archived downloads accept only `/api/careon/facturatie/facturen/<uuid>/pdf` on the module's exact HTTPS origin, refuse redirects/query/credentials, require `application/pdf`, enforce a 12 MiB streamed limit plus PDF magic, and remove the random temporary directory after sharing. Generated files use an exact versioned channel: only a Facturatie PDF or Directie sample CSV with canonical base64, safe basename, exact MIME/module and valid PDF/UTF-8 signature is accepted; no token or header crosses the channel. WebView camera/microphone/geolocation requests remain denied and the package adds no broad-storage permission. Normal browsers retain their existing blob download. Flutter analyze and **36/36** tests pass; debug/release APKs compile. The 52,909,354-byte unsigned release APK hashes to `42558815541EF5F4D63498E08EE0558B647D53ECD79C3D4896F921B9E6E2B7B8` and exposes only the expected notification/FCM/network/wakelock permissions. Careon `9b2b5f3` deploys the browser/native bridge; production probing then found a correct `401` PDF denial carrying an unsuitable public cache label. Follow-up `a914812` makes every PDF success/error/denial `private, no-store`, `Vary: Cookie`, `nosniff`, and locks this into the mobile CI contract. Full CI passes (**1000/412/145/103/70/34**, zero npm vulnerabilities), the 106-route build is green, and Vercel `dpl_outqmGNSX2yU3uBunNDQVY5mKkLm` is exact `a914812`/Ready. Live readiness is `200`; anonymous module/PDF probes are `401` with the correct cache and vary boundaries. This is code/build acceptance, not physical-device acceptance: real signed Android/iOS upload/download/share flows, account switching and offboarding still require client-owned devices and signing.
- **22 Aug 2026 — G10 isolated native JaaS bridge proof committed; gap in progress:** sibling `humhub-meeting-modules` now exists at local commit `2b35295`. Its Flutter 3.47.1/Dart 3.13.1 spike accepts only module `yaaz`, exact `https://8x8.vc`, `<AppID>/<single-level-room>`, a canonical three-part RS256 JWT whose key/app/room/audience/issuer/subject/`context.user.id`/time claims match, and explicit false values for recording, transcription, livestreaming and outbound calling. Invalid/oversized/extra-field requests fail closed, the confirmation window is protected from concurrent joins, and only sanitized lifecycle states return—never the token, URL, participant details, raw errors or PII. WebView media requests/external navigation are denied. Analyze is clean and **12/12** tests pass. Strict AAR/R8 Android debug/release compilation passes with API 26 minimum/36 target; the final 131,671,189-byte deliberately unsigned APK hashes to `F2C4908EA50F9E73BE122E1D49153EEB2AA2890D639766A9F4BDD87064B0863C`, has backup/cleartext off and excludes Jitsi's optional calendar, managed-call, screen-capture and install-referrer permissions. The shipping shell artifact/hash/permission set was unchanged at this stage and still had no camera/microphone access. Upstream Jitsi 13.1.1 currently requires narrow compile-SDK/Parcelize workarounds and warns about future built-in Kotlin migration; these are documented. No live meeting was attempted. JaaS tenant/AppID, the production native SDK adapter, signed physical Android/iOS join and interruption/audio/permission acceptance remain; the later dormant shell-contract integration is recorded below and recording/transcription/AI stay outside this increment.
- **22 Aug 2026 — G10 server authorization/signing foundation committed dormant; gap in progress:** sibling `humhub-meeting-modules` commit `32d4484` adds installable HumHub `meeting-core` **0.1.0**. It requires an enabled YAAZ account with exactly one `careon` OIDC binding and an explicit participant row for the literal-room session; every token request rechecks the account, binding, invitation, session state and bounded join window. The server reads a minimum-4096-bit RSA key from an absolute owner-only file and returns a 15-minute `private, no-store` bridge contract whose JaaS claims bind the exact Supabase subject and omit e-mail. Recording, transcription, livestreaming, file upload and outbound/SIP calling are false; database checks independently fix recording to zero and enforce session windows/status, invitation uniqueness and roles. All PHP sources lint in the pinned HumHub runtime; the pure crypto/contract suite is **46/46**, the rollback-only HumHub integration suite is **13/13**, a world-readable key fails health while mode 0600 succeeds, disabled health returns `MEETINGS=DISABLED`, guests redirect to login, and the signed-in admin-only prepared page has no navigation entry or browser warnings/errors. The local test migration is installed; no production source/config/schema, JaaS account, real key/token or shell media permission changed. Production packaging/parity/backup, tenant/DPA/region, native shell bridge and signed physical Android/iOS call acceptance remain before activation; Phase 3 remains separate.
- **22 Aug 2026 — G10 reproducible package and dormant production deployment accepted; gap in progress:** `humhub-meeting-modules` `7aeb52f` builds byte-identical `careon-meeting-core-0.1.0-7aeb52ff23ce.tar.gz` archives (SHA-256 `0a98c0e32dd5d808a66f250333db0fa5f9f41e4d3b821994965d42ea80c681e3`) and validates the extracted artifact in HumHub/PHP, not merely the working tree. `platform-deploy` `ca39055`/`0d2cf64` keeps editable meeting source outside the deploy repo per D18 while pinning source/version/archive/tree hashes. Its installer rejects missing/wrong/tampered/link/special/unsafe/private-key-shaped packages and any runtime whose meeting/member-create flags are not both explicit `0`; production mode creates and verifies a fresh DB + `/data` backup before an atomic swap with bounded rollback. A truly absent local install applied the migration from zero. Production then installed after backup `20260822-023918`; source/tree health, **13/13** rollback-only runtime checks, scheduled health service, active timers, zero staging/rollback residue, guest 302, login 200 and zero post-success error lines passed. The module is enabled only so schema and monitoring cannot drift; `JAAS_MEETINGS_ENABLED=0`, `JAAS_MEMBER_CREATE_ENABLED=0`, no JaaS key/account/token and no UI navigation keep it functionally unavailable.
- **22 Aug 2026 — G10 production-shell contract integrated dormant; gap in progress:** shell `402e8f5` moves the accepted meeting request/parser and concurrency/confirmation boundary into the shipping repository without linking the native Jitsi SDK. The WebView registers `CareonMeetingBridge` only when an explicit build request and reviewed SDK adapter are both present, for module `yaaz`, and a message is additionally refused unless the current main frame is at the exact YAAZ HTTPS origin. The JaaS contract binds the current Supabase UUID, literal AppID/room, RS256 header, audience/issuer and a maximum two-hour validity while requiring recording/transcription/livestreaming/outbound calling false; only fixed lifecycle states and mute booleans can return. Flutter analysis and **49/49** tests pass, including the guard proving the Phase-1 binary cannot expose the channel through its flag alone. Android debug/release builds pass; the 50.5 MB deliberately unsigned release APK hashes to `B55A6097E115413D5A5B21DCDADD6A8F9CFE69ABC40AF926E935E50EAD06292B`, remains application `com.careonpulse.app`, API 24/36, and contains no Jitsi artifact or camera/microphone/calendar/managed-call/media-projection/location/broad-storage permission. iOS source still declares no camera/microphone usage string. No production deployment/configuration or credential changed. G10 remains open for the TGC-owned JaaS/DPA/region decision, server secret, pinned native SDK adapter, narrow media declarations and signed physical Android/iOS call acceptance before activation.
- **22 Aug 2026 — YAAZ exception-log context hardened:** platform `6b1f44c` upgrades `careon-sso` to **1.3.1** after callback diagnosis showed that HumHub's container maps deployment variables into PHP `$_SERVER` and Yii's default exception context copied those globals into the persistent app log. The handler clears every target's `logVars` before request processing while retaining the exception and stack. The suite is **38/38**, the runtime asserts two hardened targets, and the recurring health gate additionally proves exact SSO source/runtime parity. A fresh verified production backup preceded rollout; host health is green and the module hash is `8a063cf2...c899`. A harmless state-bound invalid callback appended a 2,163-byte diagnostic entry with zero secret/cookie context markers. Existing server logs/backups remain protected operational material; there is no evidence of external disclosure.
- **21 Aug 2026 — register created:** consolidated the professional-readiness audit and recorded G01's group-gated JIT recommendation. No authentication behavior or confirmed blueprint decision was changed.
- **21 Aug 2026 — G01 approved and started:** owner approved the group/app-role-gated JIT design. Blueprint v2.4 records confirmed D22; G01-A implementation started fail-closed pending migration, Entra app-role assignment and verification.
- **21 Aug 2026 — G01-A database boundary installed dormant:** Supabase migration `20260821131342_entra_jit_membership` is registered in the live EU project. The RPC is `SECURITY DEFINER`, has an empty search path and grants execute only to `service_role`. A rollback-only live transaction proved one eligible tenant member creates exactly one `member` membership and one audit event, a repeat is idempotent, a guest (`acct=1`) creates nothing, and `authenticated` cannot execute the RPC. No JIT environment flag has been enabled.
- **21 Aug 2026 — G01-B application and cross-plane surface built:** an org-admin-only `/api/org/entra-members` route and Directie `Microsoft-medewerkers` panel reconcile the configured eligibility group with Careon identity/membership, YAAZ account/last login and personal Microsoft 365 connection presence. The Entra connector is separate from D20/D21, app-only, group-scoped, read-only and fail-closed. YAAZ `careon-m365` v0.3 exposes only a bounded OIDC/account status projection through a separate bearer-protected endpoint; it never selects provider tokens and degrades independently. Verification: Careon full CI green (998 product, 412 production, 145 assistant, 98 runtime-hardening and 34 queue checks), npm audit zero vulnerabilities, **130/130** browser tests; YAAZ **110/110** rollback checks plus live local 503-disabled, 401-wrong-bearer and 200-correct-bearer probes. The code is deployed on both planes but stays unavailable until its separate production configuration is deliberately activated. Module-entitlement columns and tenant activation remain.
- **21 Aug 2026 — Entra inventory and first activation step:** the existing dynamic security group `Alle gebruikers` contains 66 direct users and its rule is effectively “All Users”; it is therefore not an acceptable Careon eligibility source. After D22 approval, the enabled Users/Groups app role `Careon.User` was created successfully on the existing single-tenant `Careon Pulse — login` registration; this did not assign or grant access to any employee. Pending tenant actions are a dedicated curated group/direct assignment list, optional ID-token claim `acct`, assignment-required policy, explicit assignments and denied/success acceptance accounts.
- **21 Aug 2026 — G16 closed and G01 fail-closed production baseline deployed:** Careon commit `51bf605` and platform commits `c21da22` + LF portability fix `35d7124` are pushed; both working trees equal `origin/main`. Vercel's Git integration built `51bf605` as Ready/Production in 1m03s, with Microsoft login still enabled and all three new JIT/directory flags absent. YAAZ `careon-m365` 0.3.0 was deployed from the tracked archive after a verified backup at `/opt/platform-deploy/backups/code-20260821T134803Z-pre-c21da22.tar.gz`; host/runtime module manifests both hash to `d187521c491306a9133eec69eff7fae80889c62cad68221f7b63bc71b82e578a`, all containers are healthy, Graph read health is green with writes off, the normal module route redirects to login and `/microsoft-365/internal-directory` returns the intended disabled `503`.
- **21 Aug 2026 — TGC licensing and identity reconciliation:** live Entra inventory shows 46 Microsoft 365 Business Standard licences and 1 Business Premium licence. Microsoft requires Entra ID P1/P2 for group-based enterprise-app assignment, so using one group for the broader workforce is not licence-compliant today; direct app-role assignments are the approved D22 fallback. Exact comparison found Hicham, Wida and Zairo as matching Entra members, while `hassan@tgcgroep.nl` has no Entra object and retains Careon password access. Zairo already had `Default Access`; no new assignments have been submitted yet.
- **21 Aug 2026 — Directie connector adapted to current licensing:** the fail-closed read connector now accepts either the future group source or direct assignments to the one configured service principal/app-role. The direct mode reads `/servicePrincipals/{id}/appRoleAssignedTo` with application `Application.Read.All`, filters the exact role and resolves only assigned users through Graph batches capped at 20; `User.Read.All` supplies their status. Pagination is restricted to the exact configured Graph path, bearer-carrying requests reject redirects and no write methods exist. Full CI, production build and an isolated mocked token → assignments → batch test are green.
- **21 Aug 2026 — Entra login eligibility activated:** the optional ID-token claims `acct`, `email` and `xms_edov` are configured on `Careon Pulse — login`; enterprise-application assignment is required. Hicham, Wida and Zairo are now directly assigned to the exact `Careon.User` role, replacing Zairo's former `Default Access`. This authorizes only Microsoft sign-in/JIT eligibility and never grants a Careon administrator role.
- **21 Aug 2026 — full Office identity inventory connected:** the dedicated app-only registration `Careon Pulse — directory` (`a79ef35f-a757-4b2b-a26f-759be195f560`) has only the application permissions `User.Read.All` and `Application.Read.All`, both tenant-admin consented. A live client-credentials probe returned all 66 Entra users and exactly 3 `Careon.User` assignments. Directie now maps the complete inventory and displays account, licence and Careon-eligibility state separately; its access token and client secret remain server-only.
- **21 Aug 2026 — G01-C/G15 lifecycle boundary installed:** live migration `20260821141805_entra_lifecycle_reconciliation` adds a force-RLS, service-role-only lifecycle ledger and two bounded reconciliation/finalization RPCs. Disabled/guest identities become immediate candidates; role removal or deletion requires two consecutive complete snapshots; organization/platform administrators are exempt. Rollback-only live verification proved the threshold, candidate creation, audit finalization and denial to authenticated clients. Careon bans are rechecked on every server request; an already-issued JWT is not described as cryptographically revoked before expiry.
- **21 Aug 2026 — YAAZ lifecycle 0.4.0 deployed safely:** the separate bearer-protected lifecycle endpoint can disable/reactivate the exact Careon OIDC subject and transactionally delete its encrypted personal Microsoft connection. Production is healthy with Graph reads green and writes off; a non-mutating unknown-subject probe returned `not_found`. A pre-change backup is retained at `/opt/platform-deploy/backups/code-20260821-pre-m365-0.4.0.tar.gz`, and authoritative host/runtime module trees match hash `38c745d0304b3238d51ab5658abb927ba923a341f3332daa80a69427e2766112`. The rollout detected a stale local production dotenv, restored the authoritative server copy before continuing, and added merge/parity tools to prevent recurrence.
- **21 Aug 2026 — activation gates green before Careon release:** Supabase and Vercel production configuration now contain the separate JIT, directory and lifecycle settings without reusing the personal Graph connector. Careon validation is green at **998/998** product, **412/412** production, **145/145** assistant, **102/102** runtime-hardening and **34/34** queue checks; npm audit reports zero vulnerabilities and the Next.js 16.2.11 production build succeeds. Remaining G01 acceptance is a live Careon deployment, Directie inventory inspection and one real eligible employee's first Microsoft login; controlled real-user offboarding remains G15 acceptance.
- **21 Aug 2026 — G01 production inventory and Microsoft login accepted:** Careon commit `4063f4d` and platform commit `334835a` are on `origin/main`; Vercel built `4063f4d` Ready/Production in 49 seconds. Live checks return liveness `200`, Microsoft OAuth `303` to the exact Supabase/Azure path and unauthenticated reconciliation `401`. As Zairo, a real logout → Microsoft sign-in → module-launcher flow passed; the existing `org_admin` membership was preserved and never replaced by the JIT default. The live Directie view shows 66 Microsoft identities, 3 eligible, 3 active Careon matches, 0 eligible first logins pending, 7 guests and 7 disabled accounts. Because Hicham, Wida and Zairo all pre-existed in Careon, production creation of a brand-new `member` remains an explicit acceptance item even though the transactional JIT creation path is rollback-tested.
- **21 Aug 2026 — new-employee Careon JIT and G15 offboarding production-accepted:** tenant-wide admin consent is granted for only the login app's delegated `email` and `User.Read` scopes. A disposable active tenant member was directly assigned `Careon.User`; its first Microsoft login created exactly one Supabase Azure identity, one TGC membership with role `member`, and one `org.user.jit_provision` audit event, then landed on `/modules`. `/api/auth/session` proved no superadmin, financial or invoicing access, and an attempted `/dashboard/beheer` visit redirected to the ordinary dashboard. The acceptance run exposed and fixed a current-PostgREST compatibility defect: migration `20260821155300_current_postgrest_jwt_role_guard` now reads the signed role through `auth.jwt()` while preserving service-role-only grants; the same guard protects both lifecycle RPCs. The first complete snapshot observed 67 identities with 4 eligible, 3 managed ordinary members, 1 exempt organization administrator, no actions and no errors. No real employee was offboarded.
- **21 Aug 2026 — clean YAAZ first launch and cross-account isolation accepted:** the disposable Careon member launched YAAZ and received a distinct YAAZ account and exact Careon subject; Zairo remained a separate active account with his original subject and personal Microsoft connection. The first adversarial attempt exposed HumHub's unsafe sticky-account linking behavior. A recoverable pre-change code/database backup was taken, the single corrupt test binding was removed, and platform release `6c2c813` shipped `careon-sso` **1.1.0** plus `careon-m365` **0.4.2**. The SSO guard now validates subject, issuer, expiry, verified e-mail and organization before any account mutation; ambiguous lifecycle bindings return explicit `409 refused` and cannot disable the wrong account. A clean repeat then created the separate employee account. Verification is **10/10** SSO identity checks and **125/125** rollback-only Microsoft 365 checks.
- **21 Aug 2026 — G01 and G15 closed with controlled cross-plane offboarding:** disabling the disposable Entra employee produced exactly one successful `account_disabled` action. The existing Careon session immediately returned `authed:false`; the existing YAAZ session redirected to login; the YAAZ directory marked only the disposable identity `disabled`; Zairo remained `active` and Microsoft-connected. The lifecycle ledger finalized without error. The Entra test user was then deleted (recoverable for 30 days), the temporary Supabase Auth identity and TGC membership were removed, and the audit ledger was retained with a null user reference. A final complete reconciliation observed 66 identities and no actions. Production source/runtime hashes match for `careon-sso` (`410137b...`) and `careon-m365` (`c841bc0...`); all three containers are healthy. Assignment-required and exact tenant/member/verified-email enforcement, atomic idempotency/role-isolation tests, the clean first-launch path and this live revoke path complete the G01 acceptance matrix.
- **21 Aug 2026 — Microsoft made the primary whole-dashboard login:** Careon commit `a47399e` is Ready/Production. The public `/auth/v1/login` screen now leads with a high-contrast **Inloggen met Microsoft** action, explicitly recommends the Microsoft 365 work account for employees, and labels the password path as management/exception access. A clean production logout showed the new first screen; the primary action redirected to the exact TGC Microsoft tenant and Zairo completed the flow back to `/modules`. This applies to the Careon dashboard and launcher, not only YAAZ. Full `verify:ci`, npm audit and the Next.js production build are green.
- **21 Aug 2026 — G03 calendar-only write rollout deployed and partially accepted:** platform commit `a9eb4f8` ships `careon-m365` **0.5.0** with independent mail/calendar/file flags, an event editor for title/location/timing, and conflict-safe update/delete using a fresh Graph ETag plus `If-Match`. Tenant admin consent adds only delegated `Calendars.ReadWrite`; `Mail.Send` and `Files.ReadWrite.All` are absent and disabled. Production was backed up before deployment, host/runtime module hashes match, all containers and `careon-m365/health` are green, SSO is **10/10**, and the rollback-only Microsoft 365 suite is **153/153**. Zairo renewed consent and proved live create → integrated-calendar visibility → edit → refreshed visibility. The exact disposable event remains present pending the required action-time confirmation for its live cancellation; G03 stays `In progress` until cancel/absence is evidenced.
- **21 Aug 2026 — G13 production fixtures removed and closed:** platform commit `94b5304` adds the console-only `careon-demo` **1.1.0** inventory/cleanup boundary. It defaults to dry-run, requires exact user/content SHA-256 signatures plus a fixed confirmation phrase, refuses auth/Microsoft/space drift and runs transactionally. A full local DB + `/data` backup→cleanup→restore rehearsal ended `LOCAL_G13_CLEANUP_RESTORE=OK`; the rehearsal exposed and fixed `restore.sh` attempting to `exec` into a stopped app container. Before production, verified 0600 backups were captured at `backups/db-20260821-175424.sql.gz`, `backups/humhub-data-20260821-175424.tar.gz`, `backups/code-20260821-175424-pre-g13.tar.gz` and `backups/env-20260821-175424-pre-g13`. Production preflight matched exactly, then removed 5 fictional users, 11 posts, 9 comments, 22 likes, 5 local events, 4 participants, 3 fixture conversations/14 entries and 29 derived activities. Independent postconditions proved 0 fixture users/content/messages while preserving the TGC Space, 5 real memberships, 5 Careon OIDC bindings, 1 Microsoft connection and Zairo/Wida. `careon-demo` is disabled, the content index and caches are rebuilt, containers/M365 health are green, and Zairo's browser shows an empty dashboard, empty TGC feed, empty conversations, only five real platform accounts and no local demo-calendar titles. The separate Outlook acceptance event remains solely under G03.
- **21 Aug 2026 — G17 host operations activated and Careon readiness repaired:** platform `8bc6003` adds strict backup-pair verification plus tracked systemd services/timers. Local success and negative tests prove fresh archives pass while mode-644 and stale pairs fail. Production timers are enabled/active; the first health run returned `HEALTH=OK workers=4`, and the first service-run backup created mode-600 pair `db-20260821-181306.sql.gz` + `humhub-data-20260821-181306.tar.gz`, passed archive/member/trailer checks and recorded `Result=success`. The host gate covers Compose health, real MariaDB/Redis queries, internal/public TLS, scheduler/workers, M365 health and ≤36-hour backup freshness. The inventory also found and removed one exact orphaned interactive `careon-demo` disable process. Separately, Careon's public readiness exposed the missing dedicated assistant safety salt (`database` and `maintenance` were already green); a new 256-bit sensitive Production/Preview salt was added without exposing its value, and the existing Git release was redeployed as Vercel `dpl_4xhaAJt44UctdL35nbNmzUM13cKu`. Readiness is now `200` with database/assistant/maintenance/reachability all true and the first 15-minute warning/error scan is empty. Platform `1f292fd` adds an hourly, secretless GitHub smoke over Careon live/readiness/Microsoft-login and YAAZ TLS/login; its exact status matrix passes locally, while the first scheduled GitHub execution remains to be observed. G17 stays `In progress` for R2/off-site copies, secondary invoice storage backup, final-domain alert ownership, isolated production-like restore and secret-rotation operations.
- **22 Aug 2026 — G17 Facturatie Storage boundary live and accepted:** Careon `868341a` and migration `20260822234500_facturatie_storage_backup_boundary` (live Supabase record `20260822011036`) make archived PDF path/hash/byte/time metadata all-or-none, constrain SHA-256/size/organization path and pin the private `facturen` bucket to PDF/PNG with a 25 MB ceiling. The service-role console boundary recursively reconciles only archive metadata with every Storage object and fails on missing/orphaned/cross-org/malformed/size/MIME/signature/hash drift; its optional local snapshot refuses repository paths and overwrite and requires an explicit plaintext acknowledgement for an approved encrypted volume. The credential-free matrix is **33/33**. Production preflight and postflight agree on zero invoice metadata rows, zero settings and zero objects; all four constraints are validated, Storage RLS is active and the no-write byte verifier returned `FACTURATIE_STORAGE_VERIFY=OK objects=0 bytes=0 bucket=private`. Vercel `dpl_8C48j6JDwfR18mnmHRihykvUppeV` built exact `868341a` Ready on every production alias; live/readiness are `200`, Microsoft login remains the expected `303`, anonymous PDF access remains `401 private, no-store`, and the first warning/error scan is empty. No invoice content was logged, written locally or sent externally. This closes the missing code/integrity boundary, not the RPO: client-owned encrypted off-site storage, scheduling and an isolated non-empty restore drill remain G17.
- **22 Aug 2026 — G17 encrypted Facturatie secondary-copy boundary deployed dormant:** Careon `4882ffe` introduces a dedicated D19 R2 configuration rather than reusing the D8 YAAZ destination. It accepts only complete EU-jurisdiction configuration, pins the S3 client dependency exactly, encrypts each validated PDF/logo and the logical manifest with AES-256-GCM, binds authenticated context to run/key ID/type/opaque HMAC key, uses conditional no-overwrite writes and publishes the metadata-only completion marker last. The remote verifier needs no decryption key; exact-stamp fetch additionally checks R2 metadata, ciphertext hashes/sizes and every original path/size/MIME/signature/SHA-256 before atomically publishing a new local snapshot outside the repository. It requires explicit plaintext-on-approved-encrypted-volume acknowledgement, never writes Supabase and exposes no delete operation. Negative coverage is **64/64** including partial/non-EU/bad-key configuration, tamper/replay, traversal, MIME/type drift, forged timestamps/totals/keys, duplicate objects and publication-order/size guards. Full CI passes **1,870** deterministic checks plus 548 tracked-file hygiene checks, zero dependency vulnerabilities, all 107 build routes and **130/130** Playwright scenarios. Live source verification remains `FACTURATIE_STORAGE_VERIFY=OK objects=0 bytes=0 bucket=private`; off-site verify is `DISABLED required=0`, and a no-config upload fails before any external write. Vercel `dpl_Ekw3QfBbvKJUwAfoYSbgumLYTv2K` built exact `4882ffe` and is Ready on every production alias; live/readiness are `200`, Microsoft remains `303`, anonymous monitor access remains `401 no-store/nosniff`, and initial error/HTTP-500 scans are empty. No invoice bytes were copied. G17 remains `In progress` for the TGC-owned bucket/token, external key custody/lifecycle, an operator schedule and an isolated non-empty fetch/restore drill.
- **22 Aug 2026 — public database privilege boundary hardened and production-accepted:** Careon `221a986` plus migrations `20260822235000_security_invoker_boundaries` and `20260822235500_revoke_legacy_facturatie_rpc` remove the two actionable Supabase advisor findings. `careon_agenda_state_public` is now a `security_invoker`/`security_barrier` view over a schema-private, exact-organization redaction function; rollback-only and live checks preserved the ordinary-member and administrator row counts and projection digests exactly. Facturatie finalization and crediting now call `careon_factuur_definitief_maken_service` only with the service role after `requireOrgAdmin()`, and the database independently validates `p_actor` as the same organization's `org_admin` or a platform administrator. The legacy authenticated privileged RPC is absent; the replacement denies `anon` and `authenticated` and allows only `service_role`. Full CI is green (**1000** product, **412** production, **145** assistant, **105** runtime, **70** mobile, **33** Storage and **46** TGC queue checks; zero npm audit findings), and the 106-route build succeeds. Vercel `dpl_AUeRQZEDRxx1tfefCHKDLjRpiQ8W` is exact `221a986`, Ready on every production alias. Readiness is `200` with every check true; Microsoft entry is `303` to the exact Supabase project; anonymous invoice PDF access is `401 private, no-store`; Zairo's live Facturatie page exposes the admin action and empty administration with zero browser warnings/errors. The database advisor is now clear of both the security-definer view error and authenticated privileged-RPC warning. Its remaining RLS-with-no-policy notices are intentional fail-closed service tables. Supabase Auth separately still reports email OTP expiry `10800` seconds and leaked-password protection disabled; both are tracked for an explicitly confirmed access-policy change.
- **21 Aug 2026 — G12 production administration cleaned and hardened:** a privacy-minimised inventory proved TGC had no saved sender settings or contacts, five concepts, zero issued invoices, zero PDFs/storage objects and zero mail attempts. All five concepts shared the exact safe boundary `concept + no customer/contact + no number/date/issuance/pdf/mail`; one known demo signature was present and all came from the development period. A transaction locked the rows, required count `5`, SHA-256 `ed9a3f0e...386b5` and the semantic boundary, then removed exactly five and wrote one metadata-only `operations.facturatie_cleanup` audit event. Postconditions are zero TGC invoice rows and zero bucket objects. Supabase migration `20260821182849_facturatie_fk_indexes` adds leading indexes for `contact_id` and maillog `factuur_id`; the advisor no longer reports either missing foreign-key index. Careon commit `c1f4dac` is Ready/Production as Vercel `dpl_ThUvnG56iWcWmFzik52NZnXeNhey`; full CI is green (998 product, 412 production, 145 assistant, 103 runtime and 34 queue checks), npm audit reports zero vulnerabilities, the Next.js build succeeds, readiness is fully green and the first warning/error scan is empty. G12 remains `In progress` only for owner-supplied legal/company/contact data, the Resend DPA/sender domain and the controlled first-invoice acceptance.
- **21 Aug 2026 — G08 unified web navigation closed:** platform runtime release `5523fde` upgrades `careon-sso` to **1.2.0** and adds an authenticated YAAZ top-menu **Modules** entry to `https://www.careonpulse.com/modules`; source/test commit `81fc34d` preserves the reusable acceptance check. The destination validator accepts only an HTTPS `/modules` URL (localhost HTTP in development), rejects credentials/query/fragment/wrong paths, hides the entry on invalid configuration and disables cross-origin PJAX. A verified production DB + `/data` backup preceded rollout. SSO/navigation tests are **20/20**; an authenticated local HTTP acceptance proved the rendered menu id, exact configured target and PJAX prevention. Production module source/runtime hashes both equal `0d6f9941...9c4`, `careon-sso` reports 1.2.0 enabled, the configured Careon launcher returns the expected authenticated-route redirect for a guest, YAAZ login is `200`, and the host health gate is green. Careon's dashboard and Facturatie module links plus existing Careon → YAAZ OIDC handoff complete the web round trip; native shell/deep-link work remains separately tracked under G09.
- **21 Aug 2026 — G02 safe Outlook message-detail increment deployed dormant:** platform release `a67d04a` upgrades `careon-m365` to **0.6.0**. `M365_GRAPH_MAIL_CONTENT_ENABLED` independently replaces `Mail.ReadBasic` with delegated `Mail.Read`; the feature is deployed with that flag `0`, so production consent/token behavior and the existing inbox-metadata view are unchanged. The internal page performs one bounded GET with `Prefer: outlook.body-content-type="text"`, validates/URL-encodes the opaque message id, normalizes content to at most 200,000 characters of inert text, HTML-encodes the view and never fetches MIME, attachment bytes or internet headers or sends a write/read-state mutation. `Mail.Send` remains separately disabled. The rollback-only verifier is **177/177** and includes unexpected-HTML/tracking content, link spoofing, control-character and injected-ID cases. A fresh verified production backup preceded rollout; module source/runtime hashes both equal `ba779458...a5b5f`, version 0.6.0 is enabled, guest detail access redirects to login and host health is green. During rollout, post-deploy health exposed that the local secret master lacked the already-accepted calendar flag; the exact authoritative pre-G08 environment restored `calendar=on` before acceptance. Platform `3a79cfa` now makes the five-minute health gate fail on any drift from the 10-key tracked non-secret capability contract; current production is **10/10**. G02 remains `In progress` for explicit `Mail.Read` activation/employee reconnect and later separately approved reply/send/attachment behavior.
- **21 Aug 2026 — G05 canonical SharePoint browser live; gap in progress:** platform runtime `c0991bf` upgrades `careon-m365` to **0.7.0** with internal root/folder/back navigation, 50-item Graph paging, strict `graph.microsoft.com` next-link parsing and an optional configured-subtree ancestry guard. It selects only driveItem metadata; download URLs, file bytes, permissions, sharing links and version history do not cross the boundary. Unsafe Microsoft links and crafted folder/page tokens fail closed, while no shared drive is labeled explicitly as a OneDrive fallback. The rollback-only integration suite is **204/204**. A fresh verified production backup preceded rollout; live discovery still identifies the configured stable `All Company/Gedeelde documenten` drive and its empty root. Source/runtime module hashes both equal `f0cac1b6...fe14e`, capabilities are **10/10** and host health is green. Signed-in Zairo acceptance through the Careon launcher shows `TGC SharePoint`, the exact current-folder handoff, the professional connected-empty state and zero console errors. A Windows archive exposed CRLF in the non-secret capability policy; `98fa8ab` now enforces LF release artifacts and normalizes only the line terminator before exact value comparison. G05 remains `In progress` for approved real content/governance and a separate decision on file-write consent/upload workflows.
- **21 Aug 2026 — G04 Teams navigation repaired; gap in progress:** platform runtime `4572cb4` upgrades `careon-m365` to **0.8.0**. The Office landing page now performs exactly one query-free `/me/joinedTeams` request, matching Microsoft's API contract, instead of immediately issuing channel requests for the first four teams. A selected team UUID is revalidated against the signed-in employee's joined list before one metadata-only `/channels?$select=...` request; no channel e-mail, messages, chats or members are requested. Valid Graph channel URLs remain opaque, while missing/unsafe URLs become fixed-host official Teams deep links with the documented private/shared flags. The suite is **227/227** and SSO remains **20/20**. A fresh verified backup preceded production; module source/runtime hashes both equal `5dfc3409...a14b`, capabilities are **10/10** and host health is green. Signed-in Zairo acceptance shows all seven joined teams without the former `TooManyRequests` warning; Backoffice opens its accessible channel page and exact official Teams URL with no YAAZ module console errors or new throttle logs. G04 remains `In progress` because messaging/chat/meeting behavior still requires a separate privacy, retention and permission decision.
- **21 Aug 2026 — G11 governed Space rollout boundary live; gap in progress:** platform runtime `9dd4ab7` introduces console-only `careon-spaces` **1.0.0**. Production inventory is privacy-minimised and proves the unchanged real state: exactly one `TGC Groep` Space, five active members, one Space admin, zero pending memberships and zero content. A private manifest must map each proposed Space to one unique Microsoft Team, keep it private/invite-only with auto-add off, define exact members and at least one owner, select optional modules and record a retention-policy identifier; unknown or weaker fields fail closed. Approved manifests additionally require a review reference and RFC3339 timestamp. Plans expose only aggregate identity counts and deterministic manifest/inventory/plan hashes—never e-mail, Careon subject, HumHub user ID or raw Team ID. The production command registry independently proves only `inventory` and `plan` exist; there is no apply/create/delete/membership path. Local verification is **36/36**, M365 remains **227/227**, SSO **20/20**, and a fresh production backup preceded rollout. Capabilities are **10/10**, host health and systemd timers are green, `careon-spaces` source/runtime hashes both equal `82aa87ce...0485`, and post-deploy SQL confirms the same Space and membership state. G11 remains `In progress` until TGC IT approves the complete Team → Space → owners → exact members → retention mapping; no Graph permission expansion is needed if TGC IT supplies that authoritative list.
- **22 Aug 2026 — G11 transactional apply release prepared, production unchanged:** isolated/pushed platform branch `codex/g11-space-apply` at `3adcc4c` upgrades `careon-spaces` to **2.0.0** without merging or deploying it. The new console command is gated by default-off `CAREON_SPACE_APPLY_ENABLED`, the exact reviewed plan hash, a verified ≤2-hour backup stamp and a fixed confirmation. The plan hash additionally commits to a private digest of exact resolved identities and current managed-Space ACLs, preventing same-count user substitution without emitting member identifiers. Apply acquires an organization lock, runs serializably, protects the company-wide Space, creates/reconciles only approved private Spaces, transfers primary ownership before exact-member removal, normalizes leave policy, manages only `calendar`/`files`/`polls`/`tasks`, stores private Team/policy bindings plus identity-free audits and requires a zero-drift post-plan before commit. It exposes no Space-delete route and explicitly records that retention identifiers do not automate content deletion. The pinned HumHub/MariaDB suite passes **42/42** policy/redaction checks and **42/42** rollback-only create/reconcile/owner-transfer/module/non-member-isolation/hash-mismatch/injected-failure checks; source/runtime hash is `69e442a2...8d9d` for the tested tree. Local fixtures, bindings and audits are absent afterward. Production remains on 1.0.0 with its original one-Space ACL state and no capability change. G11 now waits on the actual TGC IT manifest before merge, dormant deployment, live-plan review and controlled backed-up acceptance.
- **22 Aug 2026 — G02 guarded reply and attachment source release prepared, production unchanged:** isolated/pushed platform branch `codex/g02-mail-reply-attachments` at `c0a0e80` prepares `careon-m365` **0.11.0** on the current 0.8 baseline and explicitly reserves 0.9/0.10 for the independent G04 line. Direct reply requires both content and `Mail.Send`, submits at most 10,000 characters of normalized plain text to the exact delegated message `/reply` endpoint, records only a fixed action kind and is not automatically retried; general send now shares that send-once boundary. The independent attachment gate requires content, adds no scope beyond `Mail.Read`, lists only bounded metadata and downloads only allow-listed non-inline normal files up to 10 MB after a second metadata check. MIME is server-controlled; response headers are no-store/nosniff/attachment; nothing is persisted. Inline, reference/item, unknown/executable and oversized files fail closed. PHP lint and **261/261** rollback-only checks pass, including ID/path injection, MIME spoofing, metadata-before-bytes, raw-size and no-retry cases. The release runbook separates content, attachments and send/reply into three acceptance phases. Production remains on 0.8.0, all new flags are pinned `0`, and no Graph permission, consent or employee token changed.
- **22 Aug 2026 — G05 canonical folder-write source release prepared, production unchanged:** isolated/pushed platform branch `codex/g05-folder-writes` at `b6ed6e3` prepares `careon-m365` **0.12.0** on the current 0.8 baseline and reserves/requires later reconciliation with G04 0.9/0.10 and G02 0.11. With the existing file-write flag, the library panel uploads into the actually open canonical folder and can create a subfolder. Both actions re-read the submitted parent and prove it remains on the configured SharePoint drive/subtree; OneDrive fallback is never writable. Filenames/folder names are bounded, upload types are server-allow-listed, bytes are capped at 10 MB, browser MIME is ignored and conflicts rename rather than overwrite. POST/PUT do not automatically retry; a 2xx with missing metadata remains accepted to avoid duplicate invitations. Audit strings contain only fixed action kinds and no destructive/sharing/ACL route exists. One initial rollback run correctly caught and repaired a root next-link endpoint regression; final PHP lint and **263/263** rollback-only checks are green. Production stays on 0.8.0 with file writes `0`; no permission, consent, token, document or folder changed.
